interface:
  display_name: "Memory Hub 投递"
  short_description: "智能区分日程、截止日、文档和生活信息并安全投递到待收录"
  default_prompt: "使用 $memory-hub-intake 提取事实，智能拆分日程、截止日、文档或生活信息，并投递到 Memory Hub 待收录。"

policy:
  allow_implicit_invocation: true
