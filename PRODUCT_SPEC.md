# Personal Memory Hub — 产品功能说明（全新项目版）

> 本文只记录已经确认的产品功能与数据规则。
> 不继承旧项目的页面结构、组件、CSS、视觉实验或实现方式。
> 产品定位：个人记忆与生活信息管理工具，不是医疗、诊断或效率评分工具。

## 1. 产品原则

- 移动端优先，第一阶段以手机界面为主。
- 本地优先，第一版不要求账号、云同步或联网服务。
- 信息应可恢复：普通删除先进入回收站，只有在回收站中才能永久删除。
- 不做 KPI、效率分数、连续签到、打卡压力或负罪感文案。
- 首页、日历、分类和“我的”共同构成主要结构。
- 全局搜索只保留一个入口，位于分类总览顶部。

## 2. 一级导航

移动端底部导航固定为四项：

1. 首页
2. 日历
3. 分类
4. 我的

二级页面隐藏底部导航，使用顶部返回。

## 3. 日历

### 3.1 月历主页

- 默认打开当前月份，并默认选中今天。
- 显示完整月历、前后月份切换、年月选择和“今天”返回操作。
- 月历玻璃面板支持左右滑动，快速切换相邻月份；不额外放置左右翻页箭头。
- 点击月历面板内左上方的月份标题，打开日期跳转窗口，用于跳转到较远日期。
- 日期数字下方显示简短农历文字；农历二十使用“廿”等紧凑写法，避免月历格内文字过多。
- 有事项的日期以底部小圆点标记，可显示 1–3 个；圆点颜色第一版不承载类型、优先级或状态等语义，不在月历格内堆叠事项摘要。
- 今天与当前选中日期是两个独立状态。
- 点击日期后，下方立即显示该日期的事项。

### 3.2 当日事项

- 一行一条，避免大型说明卡。
- 必须显示标题。
- 只有用户明确填写时间时才显示时间。
- 无时间事项不显示“全天”或空时间占位。
- 不在主页事项行显示长说明、地点或复杂元数据。
- 点击事项进入编辑。
- 删除通过滑动或长按操作进入软删除流程。

### 3.3 新建事项

- 先选择日期；选中日期后，下方当日事项列表提供普通事项的新建入口。
- 必填内容只有一条简短标题。
- 时间或时间段可选。
- 通知可选，默认不通知。
- 第一版创建普通事项时不要求选择语义类型；普通事项默认按“任务/周期提醒”处理，初始状态为“未完成”。
- 预约/活动与信息类的语义及其状态规则保留为后续扩展，不进入第一版普通事项的创建流程。
- 事项属于首页当前“今天”时，同时出现在首页今日卡片区。

### 3.4 状态与历史

任务/周期提醒：
- 未完成
- 已完成
- 已跳过

第一版首页中的“无视”操作写入“已跳过”状态；它不是删除。

预约/活动：
- 已发生
- 已取消
- 未参加（适用时）

信息类：
- 已查看
- 今日隐藏

过去的事项保留在日历中，不因日期过去自动删除。

### 3.5 周期提醒

- 从日历右上方进入独立管理页。
- 必填：简短名称、开始日期。
- 可选：结束日期。
- 默认在有效期内每天重复，也可只选择部分星期。
- 第一版不要求结构化时间字段。
- 周期提醒是一条规则；在有效期内且符合星期条件的日期，日历自动显示当天的周期事项，无需预先创建大量未来事项。
- 当天周期事项被完成、跳过或单独修改后，保留为该日期的独立历史实例。
- 单日操作只影响当天实例，不影响整条周期规则。
- 临时不处理某天周期事项时，使用“已跳过”：该事项仍保留在当天日历历史中，不提供“像从未生成过一样隐藏当天实例”的额外规则。
- 停止周期规则后，不再产生未来事项；已产生或已操作的历史实例保留。
- 删除周期规则遵循普通删除进入回收站的规则，且不得抹掉已经产生的历史记录。

### 3.6 通知

单个事项：
- 不通知
- 普通通知
- 强提醒

只有用户明确填写时间的单个事项，才可以开启系统通知。
无时间事项不主动发送系统通知，只在日历和首页中显示。
第一版周期提醒不要求结构化时间字段，因此只在应用内显示，不发送系统通知。
普通通知在设定时间发送一次。强提醒在设定时间首次发送后，按用户在“我的”通知设置中指定的间隔再次提示；事项被完成、无视或删除后停止后续提示。

不得承诺系统闹钟级能力，具体行为以平台实际能力为准。

独立的“每日检查提醒”：
- 默认关闭。
- 用户开启后可选择早上 8:00 或 9:00 作为固定提醒时间。
- 文案保持简短，例如“嘿，来检查一下今天的待办事项吧。”
- 点击后打开首页今日卡片区。
- 即使当天无事项，也可以按设置发送。

## 4. 首页

首页不是独立的数据源，而是日历中“今天”的投影。

### 4.1 今日卡片区

- 来源：首页当前“今天”的日历事项，以及符合当日规则的周期事项；两者都是日历中同一事项实例的投影。
- 不提供独立的首页卡片创建入口。
- 左右横向滑动切换卡片；有三条以上事项时初始聚焦中间卡，使当前卡左右两侧都能露出真实相邻事项。几何以 Figma 首页节点 `7:1236` 为准：430pt 画板上主卡 310×548、侧卡 170×438、间距 12pt，其他设备等比缩放；侧卡不得与主卡同尺寸。
- 当前主卡只保留居中的事项标题与“今天 · 时间”副信息；时间不做成独立胶囊，卡底不常驻手势说明。标题使用主题主文字色，不以纯黑左上角大段排版，也不另加不透明文字承载条。相邻侧卡不放彩色色块占位，只以较小玻璃卡轮廓表达轮播关系。
- 在当前卡片上滑执行“完成”，下滑执行“无视”；首页卡片不常驻显示操作按钮。
- 拖动超过明确阈值后才执行，拖动过程中显示方向反馈；开启“减少动态效果”时取消卡片飞出动画。
- 首页今日卡片只提供两种主要操作：完成，以及无视；无障碍操作菜单提供同名替代入口。
- 完成写入“已完成”；无视写入“已跳过”。两种操作都会使卡片从首页消失，但不会删除原始事项。
- 删除只能从日历的当日事项列表中通过滑动或长按进入软删除流程执行。
- 操作后提供短暂撤销。
- 首页操作直接更新日历中的同一事项实例，不删除来源。
- 首页“今天”的卡片刷新边界为每天凌晨 4 点，而非零点；日期结束后卡片从首页消失，但事项及其状态在日历历史中保留。
- 事项修改日期后，移出首页当前“今天”即从首页消失；移入首页当前“今天”即进入首页。

### 4.2 文档提醒区

- 位于今日卡片区下方，并从首屏之后开始；用户需要向下滚动才会看到，不与今日卡片争夺首屏注意力。
- 使用轻量横向提醒条，一次显示 3 条。
- 来源是用户在分类内文档列表的编辑状态中明确加入提醒池的文档。
- 排除已归档、已删除和回收站中的文档。
- 从提醒池中随机抽取 3 条；每次打开首页或用户主动刷新时重新抽取。
- 文案保持轻松，不使用催促、分数或连续天数。
- 点击打开原文档。
- 每条可选择：今天隐藏、稍后提醒、此文档永不提醒。
- “今天隐藏”只隐藏该文档当天的提醒；“稍后提醒”通过时间滚轮选择具体提醒时刻，最长可延后 24 小时，因此允许跨至次日；“此文档永不提醒”持续将该文档排除在提醒池外，恢复入口待确定。
- 第一版仅应用内展示，不发送系统通知。

### 4.3 首页底部的生活管理入口

- 文档提醒之后并排提供两个卡片式生活管理入口：冰箱、物品位置。
- 它们位于首页纵向内容的底部；正常滚动即可看到，不设置额外的下拉展开状态。
- 页面内容在这两个入口之后收口，不用人为增加大段空白。
- 待采购属于冰箱模块内的分区，不另占一级导航或分类页入口。

## 5. 分类

分类一级页只服务于文档及其分类管理；冰箱、待采购和物品位置均不进入此页。

### 5.1 分类总览

- 顶部只有一个全局搜索。
- 下方为纵向分类列表。
- 每项只显示颜色点与名称。
- 不显示数量、摘要或大卡片。
- 用户可以新建、重命名、改色、删除和手动排序。

### 5.2 默认分类

- 系统始终保留一个稳定的默认分类。
- 初始名称可为“未分类”。
- 用户可以重命名和改色。
- 内部身份稳定，不能删除。
- 未指定分类的文档进入该分类。

### 5.3 管理方式

普通状态不长期显示明显的新建按钮。

分类列表普通状态不提供左滑快捷操作，避免误触并保持列表安静。

单击分类条进入该分类的文档列表页面；长按才进入分类管理模式。

长按进入管理模式：
- 拖动排序
- 重命名
- 改色
- 删除
- 新增分类

可保留低调的更多菜单作为替代入口。

### 5.4 删除分类

删除含文档的自定义分类时，用户必须选择：

1. 把文档移到默认分类，然后删除分类；
2. 把分类和其中的文档一起移入回收站；
3. 取消。

永久删除只能从回收站执行。
当选择“把分类和其中的文档一起移入回收站”时，恢复该分类应还原删除前的文档归属、分类颜色、排序及“参与首页任务提醒”设置。

### 5.5 首页提醒参与

- 首页提醒池以文档为单位，不以整个分类为单位启用。
- 用户从分类内文档列表的编辑状态勾选文档，加入或移出首页提醒池。
- 勾选变化立即保存，不再显示额外的保存按钮。
- 文档详情页不放置提醒池开关。
- “我的 → 文档提醒池”只显示已加入的文档；在此处“移除”只移出提醒池，不删除文档。

## 6. 分类内文档列表

这是二级页面，隐藏底部导航。

- 底部居中“+”在当前分类中创建文档。
- 普通状态右上角提供“编辑”入口。
- 每行显示：
  - 标题
  - 右下角较弱的最后编辑时间
  - 存在任一种草稿时，最右侧显示一个珊瑚/红色小圆点
- 不显示正文摘要、分类名、进度条或“草稿”文字。
- 置顶文档在前，其余按最后编辑时间从新到旧。
- 编辑状态左侧复选框用于加入或移出首页提醒池，右侧省略号用于打开文档操作菜单。
- 文档操作菜单第一版至少包含修改标题和删除文档；删除必须二次确认并遵循回收站规则。

## 7. 文档

### 7.1 基本结构

- 创建文档：在当前分类点击“+” → 输入标题 → 打开文档 → 添加第一条正文记录。
- 标题输入并确认后，文档立即保存到当前分类；即使尚未添加正文记录，也显示在分类内文档列表中。
- 标题可编辑。
- 文档内容由按时间顺序排列的记录组成，旧到新显示。
- 页面顶部居中显示标题，可在左侧显示很小的分类色点。
- 更多操作保持低调。

### 7.2 记录

正式记录：
- 左上显示创建时间。
- 右上提供较弱的删除入口。
- 正文在下一行。
- 点击正文进入编辑。

新增记录：
- 最新记录之后显示虚线新增框。
- 点击后打开编辑器，同时下方再出现一个新的虚线“+”。
- 完全未输入的空编辑器不保存。

### 7.3 草稿与完成

- 新增但尚未完成的记录，以及编辑正式记录但尚未完成的内容，统一视为草稿。
- 两种草稿都自动保存，跨退出和切换页面保留，并触发文档列表中的草稿圆点。
- 每条正式记录最多保留一个编辑草稿。
- 编辑中的草稿不覆盖正式原文，直到点击“完成”。
- 完成后写入编辑时间与历史版本。
- 删除编辑草稿会恢复正式版本。
- 新增但尚未完成的记录，删除时直接移除，并提供短暂撤销，不进入回收站。
- 完全未输入内容的新增记录编辑器不保存。
- 草稿不自动过期。

### 7.4 小标题

可选插入小标题。

预设：
- 当前状态
- 下一步
- 问题
- 备注
- 自定义

移动端通过键盘附近的“+ 小标题”插入，插入后光标位于标题下方。

### 7.5 长记录折叠

- 正式记录正文超过约 8–10 行时折叠。
- 底部使用渐隐和“展开全文/收起”。
- 展开不能触发编辑。
- 草稿始终展开。
- 刚完成的记录在当前会话内保持展开。

### 7.6 删除与恢复

- 删除正式记录进入统一回收站，支持恢复到原文档。
- 删除整个文档也进入统一回收站。
- 归档与删除是不同状态。

## 8. 我的

“我的”是设置和数据管理中心，不要求账号页。

统一回收站的入口位于“我的 → 回收站”。

使用安静的分组列表，包含：

- 每日检查提醒与通知设置
- 外观与配色
- 应用设置
- 归档
- 回收站
- 导入、导出和本地备份
- 本地存储概览
- 隐私
- 关于、版本和帮助

未来只有在真正实现账号或同步后，才显示相关入口。

## 9. 冰箱

“冰箱”同时包含：
- 当前冰箱内容
- 待采购清单

可使用两个分区或分段控件。

### 9.1 冰箱内容

必填：
- 名称

可选：
- 冷藏/冷冻
- 数量文字
- 到期日期
- 是否已开封
- 备注

不强制填写到期日期。

状态操作：
- 已吃完
- 已丢弃
- 已移除
- 用完后加入采购清单

到期提醒保持克制，不使用倒计时施压。
“已吃完”“已丢弃”和“已移除”都会从当前冰箱内容移入模块专属的“冰箱历史／回收站”，而不进入通用回收站。
用户可在 15 天内从冰箱历史／回收站恢复项目；期满后自动永久删除。

### 9.2 待采购

- 简单清单。
- 可手动添加，也可从冰箱内容生成。
- 购买后从活跃清单移除。
- 可选择顺便更新冰箱内容，但不强制打开复杂表单。
- 购买后只保留简短撤销，不建立采购完成历史。
- 第一版不做费用统计。

## 10. 物品

同时解决：
- 物品放在哪里
- 家庭消耗品还剩多少

必填：
- 名称

可选：
- 当前地点（自由文本，不维护预设或可复用的位置层级）
- 数量
- 状态：充足 / 少量 / 用完
- 备注
- 配件或容器
- 图片（后续）

规则：
- 新增物品时提供“物品名”和“当前位置”两个自由输入框；名称必填，当前位置可留空。
- 耐用品不强制数量。
- 消耗品不强制精确地点。
- 位置变更保留历史，当前地点优先显示。

## 11. 搜索

- 全局只有一个搜索系统。
- 入口位于分类总览顶部。
- 可搜索：
  - 分类与文档
  - 文档记录
  - 冰箱内容与采购项
  - 物品
- 默认搜索活跃内容与已归档文档；已归档文档在结果中明确标记。
- 回收站内容不出现在全局搜索中，只能从“我的 → 回收站”查看、恢复或永久删除。
- 草稿不作为独立搜索结果；文档及其已保存的正式内容仍可搜索。
- 不为不同模块建立互不相干的搜索入口。

## 12. 数据、归档与回收站

- 本地数据使用版本化结构。
- 第一版只有文档支持归档；日历事项、分类、冰箱内容、采购项和物品不提供归档状态。
- 已归档文档不出现在普通列表，但可以恢复。
- 普通删除进入回收站。
- 回收站支持恢复和永久删除。
- 永久删除必须明确确认。
- 导出使用可校验的本地备份文件。
- 导入不得无提示覆盖现有数据。
- 重要数据变化应保留历史事件或版本记录。

## 13. 第一版明确不做

- 账号体系
- 云同步
- 社交功能
- AI 自动总结或聊天助手
- 生产力评分、连续签到、成就系统
- 周视图和复杂小时轴
- 费用管理
- 系统闹钟级承诺
- 桌面端高保真设计
