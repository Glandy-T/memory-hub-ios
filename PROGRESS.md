# Memory Hub — 当前进度交接

最后更新：2026-08-12

## 2026-08-12 Android 光学玻璃与同步月历轨道

- 公共 `OpticalGlass` 从乳白亚克力感收敛为更通透的光学玻璃：背景颗粒可读、模糊受控，并使用方向性反光、折射边缘亮线与轻内阴影建立厚度；没有靠全局提亮或单纯降低透明度模拟玻璃。
- 首页事项卡按住后根据触点位置产生约 6° 双轴透视倾斜，斜向移动会同时改变横纵轴和动态高光，松手平滑回正；横向切换、上滑完成、下滑无视以及减少动态继续共存。
- 日历月份改为前月、当前月、后月常驻三卡轨道。拖动过程中相邻月份立即同步进入；未过阈值回弹，过阈值后邻月沿同一轨道落位并完整居中，不再松手后才替换内容。
- 新增 `PRODUCT.md`、`DESIGN.md`、`docs/PROJECT_BASELINE.md` 与 `.impeccable/design.json`，把既有产品规则、正式浅色 Figma 基线、历史研究稿边界和动效约束固化为后续实现入口，避免跨任务走偏。
- 本地 Flutter 静态分析 0 问题；与 CI 相同的 72 项非视觉测试全部通过。完整测试额外发现两张 ADHD 实验页 golden 在当前 Windows 中文字体缺失时渲染为方框，已确认不属于正式 Android 光学玻璃/日历回归，未通过更新 golden 掩盖。
- 最终分支 Android Build `31581790249` 在 Android 35 完成：首页真实斜向倾斜与回正、日历真实拖动及相邻月进入、松手换月完整居中、固定签名升级对覆盖安装、启动前台状态、Fatal/无响应系统弹窗检查，并上传 APK 与证据。人工复核确认双月份标题同屏可读、9 月落位完整居中、首页高光随斜向倾斜变化、启动无遮挡。证据位于 `.artifacts/android-motion-31581790249/`。

## 2026-08-12 待收录文档分类选择

- 未携带有效分类的外部文档不再点击“收录”后静默落入“未分类”；主操作会先打开检查页，明确选择“保存到分类”后再“保存并收录”。
- 文档候选卡直接显示一个 48dp、TalkBack 可读的目标分类入口，不再把分类选择只藏在右上角编辑图标中；发送端新增 `--category-name`，可按已有分类名预选，仍须在待收录中确认。
- Flutter 静态分析 0 问题；待收录专项测试和完整 72 项非视觉回归全部通过。

## 2026-08-11 Android 触控动效与正式验收

- 首页玻璃事项卡加入随手指位置变化的轻量 3D 透视倾斜，松手平滑回正；继续保留横向切换事项、上滑完成和下滑无视，指针反馈不抢占原有分页手势。系统开启“移除动画”或辅助导航时停用倾斜。
- 日历月卡改为横向跟手拖动：未过阈值回弹，过阈值后旧月份沿手势退出、新月份从相反方向进入并完整回中；日期选中、分类管理状态、页面标题和四栏页面切换统一使用共享的克制时长与缓动。
- 四个主页面改为短距离淡入淡出切换；后台页面停止自身动画但不再误停退场过渡，修复日历后残留首页内容的问题。底栏继续沿用内部黄、粉、蓝分区拼合动效，不新增标签或导航层级。
- Android 正式工作流新增真实动效 QA：在 Android 35 上执行事项卡触控倾斜与回正、日历拖动中帧与换月落位，并保存三张截图；日历测试读取实际绘制坐标，要求拖动确实产生位移且松手后恢复原位。之后清装 release APK、直接启动主 Activity、确认前台进程、扫描致命日志并拒绝 Quickstep/无响应系统弹窗。
- 临时预检 `31461558099` 完成静态分析、完整测试、签名升级安装、真实手势、清装启动与证据上传；人工复核确认九月七列完整居中、主页卡片无裁边、启动截图无遮挡。证据位于 `.artifacts/android-motion-31461558099/`。

## 2026-08-10 Android 正式版底栏动效

- 按用户提供的小红书轮播第 5 张参考，将正式版四栏底部导航的选中反馈改为图标内部黄、粉、蓝硬分区由下向上拼合，同时轻微上提与放大；未选中图标继续使用低对比蓝灰描边，不引入参考图的英文标签或额外导航层级。
- 动效只表达当前目的地切换：进入 260ms、退出 180ms，使用自然减速曲线，不循环、不漂浮、不阻塞连续点击。Android 开启“移除动画”或辅助导航时改为即时切换。
- 底栏整体只增加 2dp 高度，以保证四个目的地均有真实 48×48dp 触控目标；语义标签与选中状态继续供 TalkBack 读取。
- 新增组件测试覆盖动画中间帧、最终状态、旧状态退场、48dp 触控目标和减少动态；更新首页、分类、日历 430×932 视觉基线。Flutter 静态分析无问题，完整 71 条测试通过。

## 2026-08-10 ADHD 独立实验版实现与验收

- 依据 Figma `12–19` 浅色正式设计实现独立 Flutter 实验入口；Android 使用 `com.glandy.memoryhub.lab` 和“Memory Hub 实验版”，实验状态、数据库与备份均使用独立键，正式入口、包名和数据不被覆盖。
- 完成首启、快速记录/待收录、任务关联、3/5/8 步拆解、步骤编辑重排、单步专注、暂停检查点、容量时间线、温和重排和常用流程。实验能力继续放在现有四栏结构中，没有新增 ADHD 主导航。
- 生成可复现的软件图标：浅色彩虹信息场与中央外部记忆枢纽；输出 Android 自适应前景/单色/密度资源、iOS AppIcon 全尺寸和 1024 主资源。依据补入 Apple App Icon、Android Adaptive Icon 与 Google Play 官方规格。
- 430×932 浏览器实际操作跑通首启、捕捉、待收录确认、拆解、专注、暂停恢复、日历与时间线；修复首页空状态对比度不足和步骤页重复拖拽柄，最终控制台 0 error / 0 warning。
- 静态分析、完整非视觉测试、视觉金图和 Web Release 构建作为本地门槛；Android Lab CI 独立执行 APK 构建，并在 Android 35 实际操作首启、快速记录、待收录、拆解、专注暂停和时间线，之后再做 Release 安装启动、前台焦点与致命日志检查。带 `[lab-only]` 的存档提交不触发正式 Release。
- 完整边界、实现地图、验证命令和推广注意事项见 `docs/ADHD_LAB_ARCHIVE.md`。

## 2026-08-10 ADHD 完整定位的 Figma-first 设计交付

- 本阶段只完成设计与交接，不向 Web、Android 或 iOS 源码安装新功能。正式视觉基线固定为白色背景、彩虹颗粒、蓝灰文字、标准光学内容表面和现有四栏导航；深色稿保留为历史研究但暂停实现。
- Figma 新增/完善 `08` 设计基础、`08.1` 组件库、`11` ADHD 规划与追溯矩阵，以及 `12–19` 正式浅色页面。共覆盖 51 张 430×932 正式屏、6 个可读回的 Figma 动效示例、7 组新增组件变体和 65 个具备作用域与平台代码语法的变量。
- 功能闭环覆盖：快速捕捉与待收录、可调任务拆解、单步专注、暂停与中断恢复、视觉时间线、容量与温和改期、截止日、无连续打卡的例行步骤包、事项关联文档/物品/位置/截止日、断链恢复、全局搜索、首次使用、统一编辑、提醒池编辑、回收站、离线/同步失败/权限拒绝、动态大字、减少动态、降低透明与简化设备连接。
- 明确不增加第五个 ADHD 导航入口，不使用聊天机器人主界面、效率分数、症状评分、连续打卡、羞耻式逾期、自动外部写入、玻璃套玻璃或颜色/手势作为唯一反馈。
- Figma 总审查确认新正式页面统一使用 Noto Sans SC、无未命名节点、默认文字不小于 12pt；关键手势均有可见替代，破坏性操作可撤销或进入 30 天回收站。参考、适配与拒绝模式已同步到 `docs/DESIGN_REFERENCES.md`，实现地图和节点 ID 已同步到 `docs/ADHD_DESIGN_ROADMAP.md`。
- 用户界面文案完成专项清洗：移除“不是失败”“已经替你留住了”“确认我理解得对不对”等安抚、拟人和 AI 对话式措辞，删除误放进手机画面的设计自证、反模式说明和重复提示。正式文案只保留对象、状态、动作和必要风险；规则与前后对照位于 Figma `449:131` 及 `docs/ADHD_DESIGN_ROADMAP.md`。

## 2026-08-09 Android 扫码配对

- 首次待收录连接改为“Web 显示二维码 → Android 扫码连接”；登录页面通过同源请求读取私有连接信息并在本地生成二维码，不再依赖浏览器下载器重新访问受保护接口。
- 设备连接接口只要求 Sites 已登录身份；站点本身已由 owner-only 访问策略限制，不再叠加容易失配的手工所有者编号校验。
- Android 增加二维码扫描页与相机权限，识别后继续复用现有连接格式、Keystore / Keychain 存储和首次同步校验；原 JSON 文件选择降为备用入口。
- Web 25 项测试与正式构建、Flutter 静态分析和 51 项非视觉测试通过。Android Build `31319957680` 完成固定签名双 APK 编译、Android 35 覆盖安装、启动、前台焦点与致命日志检查，并发布 `1.0.22` 应用内更新；APK SHA-256 为 `ACC2664B61138918C39EAFF0A9A8D992E0766AB3224022CB5E067FBEBA71FACC`。

## 2026-08-09 Android 应用内更新基线

- 建立 Memory Hub 专属 4096 位 RSA 永久发布证书；私钥与恢复信息只保存在 Git 忽略的 `.private/` 和 GitHub Actions Secrets，仓库、日志与 APK 下载页均不包含密码。证书 SHA-256 指纹为 `1F:84:7B:47:FD:B4:FB:B6:7F:D3:EB:B3:C6:7C:D0:26:66:15:74:16:7D:FF:1B:E5:41:0D:61:65:71:B3:E8:3C`。
- Android 启动及回到前台时至多每小时检查一次公开 GitHub 最新 Release；“我的 → 应用更新”支持主动检查。发现更高 `versionCode` 后在应用内下载 APK，下载源严格限制到本项目 Release，大小限制 150 MB，并在打开系统安装器前核对发布清单中的 SHA-256。
- 首次应用内更新会跳转安卓“允许来自此来源”设置；普通设备仍需在系统安装页面确认一次，无法也不会绕过 Android 安全确认。当前临时 debug 签名版需要最后卸载一次，安装本次固定签名基线后，后续版本可直接覆盖且保留应用数据。
- Android CI 改为使用 GitHub Secrets 中的永久证书，并在同一任务中先安装较低 `versionCode` 基线 APK、再以 `adb install -r` 覆盖到当前 APK，之后才进行启动、前台焦点和致命日志检查。通过后自动发布 `memory-hub-android.apk` 与 `memory-hub-update.json`，供应用直接检查和下载。
- 本地 Flutter 静态分析 0 问题，完整测试扩展为 53 项并全部通过；新增测试覆盖新版本识别、已是最新版以及不可信 APK 来源拒绝。
- 提交 `b47ed1b` 的 Android Build `31317644519` 全部成功：永久签名配置、双 APK 编译、Android 35 低版本安装、高版本 `adb install -r` 覆盖、启动、前台焦点、Fatal/MissingPlugin 日志检查、Release 发布和 artifact 上传均通过。首个固定签名基线为 `1.0.21`（`versionCode 1021`），公开更新页为 `android-v1.0.21`；APK SHA-256 为 `AE18A9046AFA25FDE379B05E8A37E437D75CADC2B986F2F549D01424B27DC5C4`，与应用读取的更新清单完全一致。启动截图已人工确认进入浅色首页而非加载页。

## 2026-08-09 Android 截止日闭环

- 更新并同步本机 `memory-hub-intake` 技能：其他 Codex 任务先抽取事实，再按用途自动拆分 `calendar`、`deadline`、`document` 与生活信息；明确禁止把截止日自动变成当天事项，混合信息可以生成多个待确认候选，只有日期确实缺失或歧义时才询问。安装版脚本同步支持 `deadline` 与 `--due-at`，官方技能格式验证和无网络干跑通过。
- 截止日从“今日日程”中独立出来：标题和日期必填，时间与备注可选；未设置时间时以当天 23:59 为截止点。首页在事项卡片之后只露出最近一项，日历日期标记和当天内容同步显示截止日，完整清单按最早截止时间排序。
- 相对时间按认知负担分层：仅日期项目显示“今天截止 / 明天 / 还有 N 天”；带时间项目在 48 小时内切换为小时、1 小时内切换为分钟，过期后明确显示逾期时长。临近程度只用小色点和文字颜色表达，不增加仪表盘或 KPI 压力。
- Android 支持截止日新增、编辑、完成、软删除、回收站恢复和永久删除；移动数据库由 schema v8 升至 v9，旧备份缺少 `deadlines` 时安全迁移为空列表，高于 v9 的数据继续拒绝。
- “待收录”协议新增 `deadline` 类型，其他 Codex 任务可使用 `dueAt` 或仅日期的 `date` 投递候选，仍须在 Android 待收录页检查后才进入正式数据。连接文案改称“配对钥匙”，明确它不是正式数据导入。
- Figma 文件新增“截止日功能 · 浅色”方案区，包含首页露出、完整清单和新建/编辑三张 430×932 画面，复用现有浅色颗粒背景、玻璃表面和统一底部导航。
- Flutter 静态分析 0 问题，完整 50 项逻辑、组件与视觉测试全部通过；Web 24 项测试和生产构建通过。本机 Android SDK 仍缺少 platform/build-tools，Release 安装与 Android 35 启动验收继续交由 GitHub Actions。

## 2026-08-09 Android 原生待收录闭环

- 其他 Codex 任务继续只向写入型 `/api/intake` 投递候选，不获得读取权限，也不能绕过用户确认。新增独立 Android 设备通道，只返回当前账号的待确认候选并只接受“收录 / 忽略”结果，不开放完整正式数据库读取。
- Web“我的”新增“连接 Android 原生版”：只有已登录的所有者可以下载连接文件。设备令牌与 Sites 私有访问凭证由托管环境提供，不进入仓库；Android 导入后写入系统 Keystore，iOS target 同步配置 Keychain entitlement，完整数据备份不包含这些凭证。
- Android“我的 → 待收录”可下拉同步、编辑、收录或忽略。编辑覆盖标题、备注、日历日期/时间、数量、冰箱存储方式和物品自由文本位置；信息不完整时先要求补齐，不会写入半成品。
- 收录按类型落入日历、文档、待采购、冰箱或物品位置。本机先安全持久化，再回传云端审核决定；候选派生的稳定编号使网络失败后的重试保持幂等，不重复生成正式内容。应用启动与回到前台会静默刷新。
- Flutter 静态分析 0 问题，完整 46 项逻辑、组件与 430×932 视觉测试全部通过；Web 24 项测试和生产构建通过。视觉基线固定了测试时钟，避免跨日后日历选中位置变化造成伪失败。
- 提交 `789f83a` 的 Web Build `31313459503` 与 Android Build `31313459496` 均成功。Android runner 完成 Release APK 构建、Android 35 安装启动、前台焦点和 Fatal/MissingPlugin 日志检查；启动截图人工复核正常。最终 APK 位于 `.artifacts/android-31313459496/memory-hub-android-feedback/app-release.apk`，SHA-256 为 `5E3B96D8D2B0021FA85EECDFDA0CF9E235B6FB3A8B0B0037B179FD1A3FA20388`。私有 Sites v13 同步部署成功，运行环境 revision 2 已启用独立设备密钥。

## 2026-08-08 Android/iOS 系统文件备份

- Flutter“我的 → 数据备份”已接入 Android/iOS 原生文件面板：导出使用带日期的 `memory-hub-YYYY-MM-DD.json` 建议文件名，导入仅展示文档类型并把所选内容复制到应用缓存后读取，不申请整盘存储权限。
- 文件恢复先限制异常大于 25 MB 的输入、解析 JSON、校验 Memory Hub schema 并展示事项/文档/冰箱/物品摘要；用户二次确认后才调用现有双副本替换流程。取消系统面板不会显示错误，也不会改变当前数据。
- 剪贴板导入导出降为“应急方式”并继续保留。系统插件错误、无效 JSON、字段损坏和高版本数据均使用中文反馈；无效内容无法进入确认弹窗或持久化。
- iOS target 补充原地打开文档和文件共享声明，兼容当前 iOS 13+ 目标；Android 通过系统 Storage Access Framework 工作，不增加传统外部存储权限。
- 静态分析 0 问题；自动化测试扩展至 41 条并全部通过，新增系统文件服务导出调用、有效文件确认后替换和无效 JSON 安全拒绝测试。提交 `62a8e52` 的 Android Build `31250022479` 已完成依赖解析、分析、41 条测试、Release APK 构建、Android 35 安装启动、前台焦点与 Fatal 日志检查，全部成功。验收 APK 位于 `.artifacts/android-31250022479/memory-hub-android-feedback/app-release.apk`，SHA-256 为 `3AE2E48CAA88B5E02ABFA8DE802CD5339295B8D6E6B0B8378E604AE3CF729CE7`；启动截图已人工复核，无黑屏、权限弹窗抢跑或系统错误遮挡。

## 2026-08-08 Android 系统通知闭环

- Flutter 移动数据库升至 schema v8，事项新增“不通知 / 普通 / 强提醒”模式；只有设置具体时间的事项才允许通知，清除时间会同步清除通知模式。
- 日历事项保存后使用设备真实时区调度本地系统通知。强提醒最多 6 次，间隔可选 15、30 或 60 分钟；事项完成、无视或删除后立即取消剩余通知。
- “我的 → 每日检查提醒”已接入真实设置页，可选择关闭、每天 8:00 或 9:00。系统权限只在用户主动开启每日检查或保存带通知的事项时请求；拒绝权限时事项仍安全保存，同时明确提示通知未开启。
- 通知设置页提供“发送测试通知”，用户授权后会立即向系统通知栏发送一条验收消息，无需等到次日定时点才能确认真机权限与通知渠道。
- Android 使用可在省电状态工作的非精确调度，不申请精确闹钟、全屏提醒、绕过勿扰或修改音量权限；已声明重启恢复接收器。iOS target 同步初始化通知中心代理，后续可在 Mac 上复用同一业务路径。
- 静态分析无问题；自动化测试扩展至 38 条并全部通过，新增通知调度/取消、清除时间降级和真实设置入口验证。本机 Android SDK 仍只有命令行工具且未安装需用户同意许可的 platform/build-tools，因此 Release 集成继续交由 Android Build 云端真启动验收。
- 提交 `c6b918e` 的 Android Build `31246683723` 已完成依赖解析、分析、38 条测试、通知版 Release APK 构建、Android 35 安装启动、前台焦点和 Fatal 日志检查，全部成功。最终 APK 位于 `.artifacts/android-31246683723/memory-hub-android-feedback/app-release.apk`，SHA-256 为 `A6768DD4DF78D346CE73820EFB6158D029B4BA2E8B479C89912EE4D62050D20D`；启动截图已人工复核，无权限弹窗抢跑、黑屏或系统错误遮挡。

## 2026-08-08 Android 冰箱与物品历史闭环

- Flutter 移动数据库升至 schema v7。冰箱食材新增开封状态、完整编辑、明确的“已用完 / 已丢弃 / 已移出”原因和移除时间；历史只保留 15 天，应用加载时安全清理过期条目，期间可一键恢复。
- 冰箱页以“冰箱内容 / 待采购 / 15天历史”三个紧凑分段承载当前内容、采购清单和恢复记录；历史页不再放新增按钮，当前食材单击即可编辑，移除操作继续支持同时加入待采购。
- 物品位置继续采用自由文本，不恢复固定区域限制；现可编辑名称、位置、数量、可选容器、备注与“收纳中 / 使用中 / 已借出 / 未找到”状态。只有位置真正改变时才追加旧位置与时间，列表菜单可查看位置历史。
- 全局搜索同步覆盖物品容器和历史位置。静态分析无问题；自动化测试扩展至 35 条并全部通过，新增冰箱编辑/原因/恢复、15 天自动清理、物品位置历史持久化，以及两个真实界面入口验证。
- 提交 `af6f4a0` 的 Android Build `31245475612` 已完成同一套分析与 35 条测试、Release APK 构建、Android 35 安装启动、前台焦点和 Fatal 日志检查，全部成功。验收 APK 位于 `.artifacts/android-31245475612/memory-hub-android-feedback/app-release.apk`，SHA-256 为 `FF9EF5B4C53ED549846D86B0F5D42C4394ED385448B1422A601270921A0ED799`；启动截图已人工复核，无系统弹窗或黑屏遮挡。

## 2026-08-08 Android 启动证据加固

- Android Build 运行 `31243512178` 已完成云端依赖解析、静态分析、30 条测试、Release APK 构建、Android 35 安装启动、进程存活和 artifact 上传，全部成功。
- 下载并人工检查启动截图时发现应用已完整显示，但模拟器系统桌面 `Quickstep` 的无响应弹窗遮住画面；日志中没有 Memory Hub 的 `FATAL EXCEPTION` 或 ANR，应用在约 4.16 秒完成首帧和 fully drawn。
- 工作流现会在截图前关闭残留系统弹窗，并额外要求当前焦点窗口属于 `com.glandy.memoryhub`，同时检查日志中不存在该进程的 Fatal Exception；以后不会再把“进程存活但被系统弹窗遮挡”当作完整视觉证据。
- 加固后的运行 `31243807616` 已再次通过分析、30 条测试、Release 构建、Android 35 安装启动、前台焦点、Fatal 日志检查和 artifact 上传；最终截图无遮挡，确认浅色首页实际完成绘制。验收 APK 已下载到 `.artifacts/android-31243807616/app-release.apk`，SHA-256 为 `4ECB744AE26BF60FB7293A8AE82D5E0CC30569EC1ACDA2BA01BCFF68E7DAD7DA`。

## 2026-08-08 Android 可操作备份入口

- “我的 → 数据备份”已从静态设置项升级为真实页面，显示当前数据概览，可把 schema v6 完整 JSON 复制到剪贴板并由用户保存到可信本地文件或笔记。
- 支持从剪贴板粘贴恢复：先解析并校验 Memory Hub schema，再展示备份中的文档/事项数量并要求二次确认；高于当前版本或格式错误的内容会被拒绝且不改变现有数据库。
- 替换恢复仍经过主副本/上一版备份轮换和写入确认，失败不会在界面伪装成成功。文件选择器形式的导入导出后续在引入原生文件依赖时补充。
- 静态分析无问题；自动化测试扩展至 30 条并全部通过，新增完整备份往返和高版本拒绝验证。

## 2026-08-08 Android 周期事项规则闭环

- Flutter 移动数据库升至 schema v6，新增周期规则集合与事项的来源规则标识；规则支持名称、开始日期、可选结束日期、指定星期和启停状态。
- 日历顶部“周期事项”不再是提示占位，已接通规则列表、新增、编辑、启停和长按软删除；删除规则停止未来投影，已经操作过的日期历史继续保留，删除的规则进入统一回收站。
- 周期规则不会预创建大量未来事项。查看具体日期时按规则投影当天事项；当天被完成、无视或单独编辑后才持久化为独立实例，后续日期仍按原规则生成。
- 静态分析无问题；自动化测试扩展至 29 条并全部通过，覆盖规则投影、操作实例持久化、未来停止和历史保留，以及真实规则管理入口。

## 2026-08-08 Android 记录草稿、版本阅读与文档归档

- Flutter 移动数据库升至 schema v5；新增记录输入会在停止输入约 450ms 后写入文档草稿，退出详情再打开仍可继续，正式发布后自动清除草稿。
- 正式记录编辑继续保留旧正文，记录菜单在存在历史时提供“历史版本”阅读面板；长记录默认约 9 行收起，可按需展开，减少详情页滚动负担。
- “我的 → 归档文档”已接通，列出已归档内容，可阅读并恢复；归档和删除继续保持独立状态。
- 静态分析无问题；自动化测试扩展至 27 条并全部通过，新增草稿跨控制器恢复及发布清理验证。

## 2026-08-08 Android 搜索、提醒池与统一回收站

- Flutter 移动数据库升至 schema v4，文档在分类级联删除时记录来源分类和删除前的提醒池状态；恢复分类时只恢复随该分类一起删除的文档，不误复活此前单独删除的内容，并还原原分类归属与提醒选择。
- 分类顶部搜索框已接入真实全局搜索，覆盖分类、活跃/归档文档、正式记录、冰箱、待采购和物品位置；排除回收站，结果可进入对应文档或生活模块。
- “我的 → 文档提醒池”已接通，只显示当前有效候选并可直接移出；“我的 → 回收站”已接通事项、分类、文档、正式记录和物品位置，支持恢复与二次确认永久删除。
- 尚未实现的设置项不再显示误导性的可点击箭头，明确标注为尚未接入或当前实际状态。
- 静态分析无问题；自动化测试扩展至 26 条，新增分类级联恢复、跨模块搜索和回收站恢复路径，全部通过。

## 2026-08-08 Android 数据安全、文档操作与提醒闭环

- Flutter 移动数据库升至 schema v3。正式记录新增软删除、编辑时间和正文版本历史；文档新增提醒静默截止时间，旧 schema 数据继续按可选字段迁移。
- `SharedPreferences` 持久化改为主副本双份轮换：每次写入前只把可解析的上一版主数据复制为备份；主数据损坏时先隔离原文并尝试从备份恢复，两份都损坏时停止写入并进入中文诊断页，不再静默初始化为空数据库。
- 控制器改为磁盘确认成功后才更新界面。写入失败时保留原有已保存状态，在所有主页面顶部显示“未安全保存”提示和重试入口，避免把只存在于内存的修改伪装成成功。
- 文档列表编辑态的省略号已成为真实菜单，支持修改标题、归档和二次确认软删除；文档详情中的正式记录支持编辑、版本留存和二次确认软删除。
- Android 首页文档提醒改为从有效提醒池随机抽取最多三篇；主动刷新会重新抽取，点击提醒进入原文档，单条支持今天隐藏、通过日期时间滚轮延后最多 24 小时，以及移出提醒池。
- 本地静态分析无问题；逻辑、数据恢复、组件与 430×932 视觉测试合计 23 条全部通过。仍未完成的 Android 功能包括统一回收站界面、全局搜索、周期事项/通知、设置页真实入口及生活模块完整历史。

## 2026-08-08 Android 原生反馈端启动

- 路线调整落实为 Flutter 单代码库：Android 先承担真实设备反馈，工程同时保留 iOS target，后续在 Mac 上继续使用同一业务与界面代码，不再把 Android 做成 WebView 外壳。
- 已按 Figma 浅色事项卡节点 `330:55` 建立浅色彩虹颗粒、克制玻璃、310×548 主事项卡、缩小真实侧卡、无文字四栏底部导航，以及“上滑完成 / 下滑无视”的原生 Flutter 首页。
- 首个移动数据库包含事项、分类、文档、正式记录、提醒池、冰箱、待采购和自由文本物品位置；schema 已升至 v2，并覆盖 v1 迁移、凌晨 4 点边界、状态持久化与生活模块闭环。
- 冰箱与物品已从入口占位升级为可持久化页面：冰箱区分冷藏/冷冻、数量、可选到期日与备注，移除时可加入待采购；物品位置不使用固定区域选择。
- Windows 工作区使用被 Git 忽略的便携 Flutter 3.44.9、JDK 17 和 Android 命令行工具。Flutter analyze 无问题，7 条逻辑/组件测试及 1 条 430×932 视觉基线测试通过。
- 新增独立 `Android Build` GitHub Actions：每次移动端变更在 Linux 上分析、测试并构建使用临时调试签名的优化反馈 APK，artifact 保留 14 天；该包用于个人真机反馈，不作为商店发布签名。本机 Android SDK 许可证仍需用户本人阅读接受，正常开发不会代替用户静默同意。
- 第一份 APK 在真机点击后立即退出。Android 35 模拟器复现并由 `logcat` 定位为 application ID 已改为 `com.glandy.memoryhub`，但 Kotlin `MainActivity` 仍留在旧包名 `com.glandy.memory_hub`，系统因此无法实例化启动 Activity；提交 `0607575` 已同步包声明与目录。
- 启动初始化现改为先运行 Flutter，再异步加载中文日期和本地数据库；初始化异常会留在中文诊断页并允许重试，不再在首帧前直接结束进程。
- Android 工作流不再只验证编译：会把同一 Release APK 安装到 Android 35 模拟器、从启动器打开、等待 10 秒、确认应用进程存活，并保存截图与 `logcat`。运行 `31239554535` 已完成分析、7 条测试、Release 构建、安装、启动、存活、截图和 artifact 上传，全部通过。
- 真机反馈发现冰箱、物品位置和分类等二级路由显示 Android 黑色窗口底。原因是透明 `Scaffold` 的彩虹背景只包在一级 Shell 内；现已把 `PigmentBackground` 提升到整个 `MaterialApp` Navigator 外层，所有现有及未来二级路由默认继承同一背景。测试逐级覆盖冰箱、物品位置、分类、文档列表和文档详情；最终运行 `31240135391` 以 9 条测试和 Android 35 真启动验收全部通过。

## 2026-08-07 ADHD 支持候选与设计流程

- Memory Hub 的长期定位是为日常 ADHD 困扰提供私人外部记忆与执行支持，而非全能资料库；低摩擦捕获、下一步动作、低能量状态、开始/截止/预计时长、温和顺延和跨模块关联均记录为候选需求，尚未进入正式 Web 数据模型或 UI。
- 先完成 PRODUCT_SPEC、Figma 与本文件已确认的基础功能与浅色视觉。任何新增页面、信息层级、核心交互或明显视觉构图的功能，必须先在 Figma 创建独立提案或流程稿、对比并确认后，才同步到代码；纯实现性修复、无障碍、响应式和错误处理可直接修改。

## 2026-08-07 Web/PWA 功能与浅色界面收口

- Web/PWA 扩展为同一套持久化与跨设备同步数据面：分类、文档提醒、冰箱/待采购、物品位置、回收站、外观与每日检查设置均不再是展示占位；旧版数据仍会迁移为可用副本。
- 日历升级为月历与当日事项组合，补齐周期事项管理入口；首页继续以中央事项卡为焦点，完成/无视保留撤销，任务状态会回写同一条日历数据。
- 分类页加入全局搜索与安静的管理模式，生活模块、文档列表和回收站统一使用浅色颗粒与克制玻璃层级；二级页面隐藏底部导航。
- Web 自动测试扩展为 22 条，TypeScript / PWA 生产构建与 `git diff --check` 已通过；测试新增 v1→v2 迁移、分类级联、周期实例、软删除并发合并、备份篡改拒绝和同步响应错误覆盖。
- 浏览器在 430×932、393×873 与 1200×900 实际走通分类与文档记录、提醒池、月历农历、周期单日完成、冰箱→采购→历史恢复、物品软删除→回收站恢复、深浅主题、同步失败和上下手势。实测发现并修复新增文档空 payload 崩溃、周期实例完成后首页重复生成虚拟卡，以及开发回退 HTML 暴露底层解析错误的问题；新浏览器会话控制台无警告或错误。
- 最终视觉收口修正了桌面事项卡偏左、手机首屏导航遮住提醒文字、底部页面切换后沿用旧滚动位置，以及日历行修复嵌套按钮后标题被旧网格压缩的问题；文档提醒现在保持在首页首屏以下，日历事项区用无边界浅色雾面降低颗粒干扰，不额外堆叠卡片。

## 2026-08-07 Web 日历事项闭环

- Web 日历从展示占位升级为真实事项流：可按日期新增和编辑事项，标题必填，时间与备注可选；未设置时间时首页显示“全天”。
- 首页与日历共用同一条事项数据。首页当前卡上滑完成、下滑无视，日历可查看完成/无视状态；软删除、完成和无视都立即持久化并提供短暂撤销。
- 横向事项轮播会以屏幕中央卡片作为真实操作对象；三条事项时默认聚焦中间卡，左右保留较小的真实相邻卡，不使用色块占位。
- 同步数据增加可选的事项状态与更新时间，旧数据无需迁移即可继续读取；多设备合并时以较新的 `updatedAt` 为准，避免旧设备把完成、无视或删除状态覆盖回来。
- Worker 对同步包改为逐项验证，不再仅检查顶层数组；损坏或伪造的条目会在写入前被拒绝。
- 本地自动验证现为 16 条测试全部通过，覆盖 intake、仓库合并、日历新增/编辑/状态持久化、较新设备状态优先及 Worker 乐观版本写入。浏览器已在 430×932 与 1200×900 验收新增、编辑、完成、删除、撤销、三卡焦点与响应式布局，控制台无警告或错误。

## 2026-08-07 跨平台待收录与 PWA 基线

- PWA 已发布为仅账号本人可访问的 Sites 应用，iPhone、Android 和桌面浏览器均可打开并安装；线上首页与脚本资源已按真实 HTTP 200 验收。
- Web 数据由纯 `localStorage` 升级为私有 D1 同步：以 Sites 转发的稳定登录用户 ID 隔离数据，整体 JSON 使用乐观版本号写入；多设备副本按稳定条目 ID 合并，已收录状态优先，避免重复或旧设备覆盖。
- 浏览器仍立即写入本地副本，断网时可以继续使用；恢复网络或重新打开应用后自动重试同步。“我的”页显示同步中、已同步或离线状态。
- D1 表结构与生成迁移保存在 `web/db`、`web/drizzle`；完整数据同步 API 只接受已登录用户的同源请求，并限制同步包大小。
- 新增 `codex-skills/memory-hub-intake` 本机技能与写入专用 `/api/intake`：其他 Codex 任务可把五类 intake v1 候选直接投递到当前账号的待收录列表。令牌采用单向用途边界，只能新增候选，不能读取整库或将候选直接变为正式内容；稳定内容 ID 使网络重试不会重复创建。
- 线上端到端验收已通过：本机技能首次投递“Memory Hub 自动验收”返回新增 1 条，使用完全相同内容再次投递返回新增 0 条且 item ID 一致。测试候选只进入待收录，可直接忽略。技能同时携带私有 Sites 通行凭证与应用级写入凭证；完整 `/api/state` 仍要求真实登录用户身份，命令行凭证不能读取整库。

- 当前路线调整为：iPhone 先使用可安装 Web/PWA，Android 原生端后续作为主要客户端；已有 SwiftUI 工程继续保留，不要求为了首次使用立即购买 Apple Developer 会员或云 Mac。
- 新增 `web/` React + TypeScript PWA。首页继承已确认的浅色彩虹颗粒、克制玻璃、四栏无文字悬浮导航、满屏事项卡和“上滑完成 / 下滑无视”；无时间事项显示“全天”，备注仍为可选。
- 新增独立“待收录”入口与审核页。支持文件或剪贴板 JSON 导入、来源与类型展示、逐条编辑、接受、忽略和全部接受；接受后按目标写入日历、文档、冰箱、待采购或物品列表。
- 新增跨平台协议 `shared/memoryhub-intake.schema.json` 与示例。协议以稳定 `item.id` 做幂等去重，外部 Codex 任务只能提交候选项，不直接覆盖正式数据。
- Web 仍用 `localStorage` 作为离线副本，权威副本已接入账号隔离的私有 D1；Android 原生端未来可消费同一 intake 协议与同步边界。
- 初始基线包含 4 条协议单元测试、TypeScript 构建、Vite 生产构建及 Service Worker 预缓存生成；后续测试已扩展到上方记录的 16 条。浏览器已在 430×932 与 1200×900 验收首页、待收录、编辑、接受、非法 JSON、分类写入及生活模块入口；控制台无错误。
- GitHub Actions 已拆分：Web/协议变化只走 Linux `Web Build`，iOS 源码与工程变化才走 macOS `iOS Build`，避免 Web 迭代触发昂贵且较慢的模拟器检查。
- GitHub runner 升级到 iOS 26.2 后，文件导出器的关闭控件会从短暂的“Cancel”切换为“浏览”层级，导致原先的关闭点击测试失效。CI 现在只验证导出器确实打开并终止隔离测试应用；取消、落盘与重新导入仍在可交互 Mac/真机验收，避免系统文案变化反复触发失败邮件。

## 2026-08-06 浅色功能基线

- 第一版开发与视觉验收暂时以浅色主题为基线；深色主题及 Figma 极光研究稿继续保留，但不阻塞功能闭环。
- 新安装在没有既有外观偏好时默认使用浅色主题；用户主动选择“跟随系统”或“深色”后仍按原规则持久化，不会在升级后被强制改回浅色。
- 文档提醒候选过滤从 `HomeView` 抽到 `AppStore.eligibleReminderDocuments`，统一排除当天隐藏、仍在稍后提醒期、已归档和已删除文档；首页继续从候选中随机抽取最多三篇。
- 单元测试新增主题默认值与持久化、提醒候选过滤、冰箱移除后加入采购并从 15 天历史恢复、物品位置历史去重及回收站恢复。提交 `15e4d8b` 的 GitHub Actions 运行 `31075180253` 已使用 Xcode 16.4 完成 Debug 编译并在 iPhone 16 Simulator 执行 10 条测试，结果为 0 failures、`TEST SUCCEEDED`。
- 现阶段不修改已确认的浅色首页排版、颗粒背景、玻璃层级、导航结构和手势规则。

## 2026-08-06 核心 UI 冒烟路径

- 新增独立 `MemoryHubUITests` target，并加入共享 Scheme；普通 `xcodebuild test` 会同时发现单元测试与 UI 测试。
- UI 测试使用 `--memory-hub-ui-testing` 启动参数，每次启动重建独立临时数据库和偏好设置，不读取或污染用户的正常本地数据。
- 首批五条路径覆盖：默认分类中新建文档、文档加入首页提醒池、新增日历事项、新增冰箱食材，以及新增带自由文本位置的物品。
- 为首页两个生活入口及冰箱/物品创建按钮补充稳定的中文无障碍名称；视觉结构和已确认的浅色排版未改变。
- Windows 侧已通过 Scheme XML、工程引用、成对括号及 `git diff --check` 检查。提交 `96a9df0` 的 GitHub Actions 运行 `31077033097` 随后使用 Xcode 16.4 / iPhone 16 Simulator 实跑：10 条单元测试和 5 条 UI 测试全部通过，0 failures、`TEST SUCCEEDED`；整轮构建与测试耗时 6 分 34 秒。
- 第二批增加第一条记录草稿发布，以及文档删除、进入“我的 → 回收站”并恢复的完整路径。首次运行准确暴露了记录正文在可访问性树中属于可编辑按钮而非静态文本；修正测试查询角色后，提交 `93e005f` 的运行 `31078383466` 完成 10 条单测与 7 条 UI 测试，合计 17 条、0 failures、`TEST SUCCEEDED`。
- GitHub Actions 对仅修改 Markdown 的 push/PR 增加 `paths-ignore`；文档交接更新不再重复占用 macOS 模拟器时间，代码、工程和工作流变化仍执行完整检查。
- 系统交互阶段新增“数据备份 → 打开系统导出面板 → 取消”路径，并在 iOS 模拟器实际通过。通知权限在 GitHub 无签名模拟器中既不稳定暴露系统按钮，也不返回可判定的授权结果；连续验证确认这属于 CI 环境限制，因此通知授权、送达与取消明确保留为签名应用/真机验收项，不再制造阻塞 CI 的易碎用例。
- 最终稳定基线为提交 `ee870e9`、运行 `31082773846`：10 条单元测试与 8 条 UI 测试全部通过，0 failures、`TEST SUCCEEDED`。期间的失败邮件均来自同一仓库的 UI 测试迭代，没有额外仓库、安全或账号异常。
- 随后补齐数据安全边界：整库替换导入会移除旧数据并修复默认分类；高于当前 schema 的备份会被拒绝且不改变现有数据；恢复级联删除的分类时不会误复活用户此前单独删除的文档或记录。提交 `ca75513` 的运行 `31083887670` 完成 13 条单元测试与 8 条 UI 测试，合计 21 条全部通过、0 failures；这是当前最新稳定基线。

## 2026-08-04 深色极光主题研究

- Figma 新增独立页面 `10 · 深色极光主题研究`（`354:74`）与首页研究画面 `首页 · 深色极光主题研究 · A`（`354:75`），未覆盖正式首页和浅色提案。
- 研究稿继承已确认的首页构图：主卡 310×548、侧卡 170×438、44 pt Medium 时间、居中标题与可选备注，并使用正式 `Navigation/Bottom` 的 Dark / Home 变体。
- 深色背景改为连续海军蓝—青黑基底；极光采用青绿、冷蓝、紫色为主的斜向宽光带，只保留极少暖粉尾色。顶部信息区保持安静，主要亮区穿过事项卡中下部，避免七色霓虹和分离色块。
- 主卡与侧卡使用低遮蔽渐变、30/24 pt 背景模糊和方向性细描边，让背景色通过材质形成层次；卡内不新增独立彩虹装饰。低饱和颗粒只作为光区质感，不与文字处于同一视觉平面。
- 首轮检查后将极光光带的亮度与色彩存在感提升约一档，同时进一步降低主卡和侧卡的遮蔽；最终画面可明确读到左侧青光与卡内蓝紫过渡，但顶部日期区域仍保持安静。
- 已分别检查整屏、主事项卡和底部导航截图；文字均为 Inter / Noto Sans SC，无异常字体、越界节点或常驻手势提示。该稿目前只用于 Figma 方向对比，尚未同步 SwiftUI。

## 2026-08-03 首页视觉定稿

- 浅色首页事项卡提案 `330:55` 确认为首页实现基线：主卡 310×548，时间、标题和可选备注统一沿中轴排布。
- 时间定为 44 pt Medium；时间与标题组约 48 pt 间距。未填写时间时显示“全天”，未填写备注时不保留占位。
- 彩色折射带下移并减弱，避免与主要文字争抢层级；侧卡继续保持较小玻璃轮廓，不使用彩色色块占位。
- 浅色提案已删除临时手绘导航，恢复使用正式 `Navigation/Bottom` 的 Light / Home 组件变体；实际工程继续由统一的 `RootBottomNavigation` 承载四个一级入口。
- SwiftUI 首页已同步上述排版值，保留左右滑动切换、上滑完成、下滑无视、减少动态效果和无障碍替代操作。

## 项目基线

- Figma 视觉准备已进入可开发状态，原生 iOS 第一阶段工程已经开始。
- 产品功能与数据规则以 `PRODUCT_SPEC.md` 为准；本文件记录当前设计状态与已确认交互。
- Figma：<https://www.figma.com/design/K2hgHAGExCF40ZtPtMY91x/Personal-Memory-Hub-%E2%80%94-Dark-iOS>
- 视觉方向：安静、清晰、留白克制；深海军蓝背景，冷蓝选中态，低饱和青紫环境光。
- 排版参考：<https://www.behance.net/gallery/242023175/UIUX-for-Wellness-Aging-Care-Mood-Meds-Tracking-app>
- 配色参考：<https://www.behance.net/gallery/210003217/Aqara-Smart-Home-App-UI-UX-Design>

## Figma 页面与关键画面

| 页面 | 页面 ID | 关键画面 |
| --- | --- | --- |
| 首页 | `0:1` | 首页主画面 `7:1236`；完整纵向内容 `251:2` |
| 日历 | `7:1291` | 月历主页 `7:2421` |
| 分类与文档 | `7:2585` | 文档列表 `130:2`；提醒池编辑 `230:4`；文档菜单 `232:8`；删除确认 `232:110`；重命名 `236:2`；文档详情 `133:2`；新建文档 `136:110` |
| 导航动效研究 | `86:2` | 仅为研究稿，当前低优先级 |
| 首页提醒与生活入口 | `106:2` | 提醒、冰箱与物品卡片 `154:2` |
| 冰箱与物品 | `164:3` | 两个独立模块的列表、编辑、选择与删除流程 |
| 我的与提醒池 | `218:2` | 提醒池管理 `218:106`；空状态 `236:220` |
| 浅色主题研究 | `260:2` | 首页浅色颗粒主题研究（单屏）`265:2` |

## 已确认的首页结构

- 首页主任务来自日历中“今天”的事项，不是独立数据源。
- 主任务保持纵向长卡和横向切换关系；不为了同时展示提醒而压缩主卡。
- 文档提醒位于主任务之后，一次展示 3 条。
- 提醒从用户明确加入的“文档提醒池”随机抽取；每次打开首页或主动刷新时重新抽取。
- 点击提醒打开原文档；单条支持今天隐藏、稍后提醒、永不提醒。
- 文档提醒之后并排展示“冰箱”和“物品位置”两个近方形入口卡片；它们是独立模块，不属于分类。
- 首页纵向内容在两个入口卡片之后结束。实现时内容高度随内容收口，只保留安全区和底部导航所需间距，不制造大段空白。
- 不再把冰箱、物品入口描述为必须通过特殊“下拉展开”才能出现；正常纵向滚动到页面底部即可。

## 分类、文档与提醒池

### 分类

- 分类普通状态为按钮式列表项；单击进入该分类的文档列表。
- 长按分类项进入管理模式。管理模式提供拖动排序、重命名、改色、删除和新增分类。
- 普通状态不提供左滑删除，避免与单击进入、长按管理形成多套手势。

### 文档列表

- 普通状态右上角有“编辑”入口。
- 编辑状态左侧复选框用于加入或移出首页文档提醒池，勾选后立即生效，不另设保存按钮。
- 编辑状态右侧省略号打开文档操作菜单，包含修改标题和删除文档。
- 删除文档必须二次确认；删除文档时一并处理其记录，并遵循回收站规则。
- 设置页的“文档提醒池”只显示已经加入池中的文档；“移除”仅移出提醒池，不删除文档。
- 空池状态引导用户回到分类中的文档列表进行选择。

### 新建与记录

- 在当前分类点击“+”，第一步只输入文档标题并创建文档容器。
- 创建后进入空文档详情；第一条记录与后续记录使用同一套新增记录流程。
- 文档详情页保持阅读与记录管理职责，不再放“加入首页提醒”开关。

## 冰箱与物品

- 首页只提供两个并排入口卡片，进入后分别使用独立页面。
- 冰箱包含“冰箱内容”和“待采购”两个分区；待采购不是一级入口。
- 物品的位置使用自由文本，不强迫用户从固定区域层级中选择。
- 删除确认弹窗保持紧凑，尤其控制上下高度。

## 我的与导航

- “我的”使用安静的分组容器，不在每个设置项下使用突兀的整宽分割线；优先通过组内间距、弱底色和必要的短分隔表达层级。
- 四个一级页面统一使用同一底部导航尺寸、位置、图标和选中态；二级页面隐藏底部导航。
- 导航动效研究暂缓，不阻塞其他页面。实际开发时再根据设备预览确认动效。

## 2026-08-02 设计清理结果

- 已从分类与首页提醒页面移除 602 个不可见旧图层，避免继续编辑时误选和文件膨胀。
- 已为文档菜单、删除确认、冰箱/物品选择器与弹窗补齐至少 44 pt 的触控区域。
- 日历可见农历文字统一提升至 11 pt。
- 最终结构扫描结果：分类、首页提醒、日历、冰箱与物品页面均无缺失字体、无小于 11 pt 的可见文字、无小于 44 pt 的反应目标、无顶层画面内的隐藏直属旧图层。

## 2026-08-02 原型连线状态

- 分类与文档页面已补齐 37 个原型入口，覆盖分类单击进入、按压进入管理、管理菜单、新增分类、文档列表、文档详情、新增记录、新建文档、提醒池编辑、文档重命名和删除确认。
- 分类页连线复查结果：无失效目标，所有带反应的触控区域均不小于 44 pt。
- 冰箱与物品页面已有 34 个有效原型入口，内部新增、编辑、选择、搜索、空状态和删除流程完整。
- “我的”页面已有设置到提醒池、提醒池返回设置的路径。
- 当前各一级模块分布在不同 Figma 页面。Figma 原型的 `NAVIGATE` 目标必须是同一页面的另一个顶层画面，因此底部导航、首页提醒到文档、首页卡片到冰箱/物品、提醒池空状态到分类编辑不能直接跨页面连线。
- 如果需要一条可从首页完整点击到底的演示原型，应新增独立的“完整原型”页面，将各模块的演示画面复制到同一页面后再连接；现阶段不为连线而复制主设计稿。

## 2026-08-02 首页完整纵向内容

- 已新增画面 `首页 · 完整纵向内容`（`251:2`），把确认过的主任务卡、三条文档提醒、冰箱和物品位置入口整合为一条连续内容流。
- 区块节奏为：主任务卡结束后 32 pt 进入文档提醒；最后一条提醒后 24 pt 进入双入口卡片。
- 双入口卡片结束后只保留内容收口和底部导航安全间距，不再复制原独立画面中的大段空白。
- 该画面是开发用的完整内容高度示意；实际 iOS 实现应使用纵向 `ScrollView`，底部导航固定或通过安全区 inset 布置，不能靠给内容硬塞空白避让。
- 视觉与结构复查：无缺失字体、无小于 11 pt 的可见文字、无内容越界。沿用现有设计字体 `Inter Semi Bold` 与 `Noto Sans SC`。

## 2026-08-02 浅色主题方向

- 已新增独立页面 `07 · 浅色主题研究`（`260:2`）与研究画面 `首页 · 浅色颗粒主题研究 · 单屏`（`265:2`），由正式单屏首页 `7:1236` 复制，未覆盖深色母版。
- 浅色主题不是简单反相：使用冷白底、深墨蓝文字、半透明白色卡片与彩色颗粒雾。颗粒是主题识别层，不照搬参考图的彩虹构图。
- 主题研究只使用标准 430×932 首屏，不再依赖完整长页面展示色彩变化。
- 已移除容易呈现为水渍的连续模糊色块，以及后续效果像脏污的稀疏矢量噪点。当前研究稿使用重新平衡构图的高密度彩粉材质图 `assets/theme/light-pigment-background-v2.png`：蓝青回到上中部，黄绿补足左中，粉红与紫色在下方左右分担重量，视觉中心落在主卡片内部；边缘由密集微粒自然消散，顶部文字区域保持较干净。
- 该画面暂为主题研究稿。确认视觉方向后，再把浅色/深色语义颜色写入设计变量并批量应用到其他页面。

## 2026-08-02 设计系统基础

- 已建立 4 个变量集合：`Memory Hub · Primitives`、`Memory Hub · Color`、`Memory Hub · Spacing`、`Memory Hub · Radius`。
- 共建立 59 个变量，其中 16 个语义颜色支持 Light / Dark 两种模式，并全部通过基础颜色别名连接；其余为基础颜色、间距和圆角变量。
- 已建立 10 个标准文字样式，覆盖日期展示、页面标题、卡片标题、区块标题、正文、标签和最小辅助信息；暂未批量覆盖现有画面。
- 已建立卡片、悬浮控件和弹窗 3 个标准阴影效果样式。
- 第一阶段校验通过：4 个集合均无模式缺值，语义颜色无别名失效。下一阶段整理底部导航、列表项、卡片和弹窗组件。

## 2026-08-02 核心组件库

- 已新增独立页面 `08 · 设计系统`（`285:2`），不直接改动现有业务画面。
- `Navigation/Bottom`（`285:131`）：Light / Dark × Home / Calendar / Categories / Profile，共 8 个变体；选中态统一使用颜色与阴影，不再依赖重复图层。
- `List/DocumentRow`（`288:46`）：Light / Dark × Default / DefaultDraft / EditingSelected / EditingUnselected，共 8 个变体；草稿红点由组件自身提供，编辑态包含提醒池复选框和 44 pt 更多操作区域。
- `Card/ModuleEntry`（`289:36`）：Light / Dark × Fridge / Location，共 4 个变体。
- `Dialog/Confirm`（`289:55`）：Light / Dark 共 2 个变体，固定 320×164 紧凑比例，双按钮各 160×64。
- 组件阶段校验通过：4 个组件集、22 个变体，数量与预期一致。

## 2026-08-02 组件试套

- 已新增独立页面 `09 · 组件试套`（`291:2`），画面 `证件文档列表 · 组件试套`（`291:3`）由正式文档列表 `130:2` 复制。
- 三条文档已替换为 `List/DocumentRow` 实例；标题、日期、78 pt 行距和分隔线与源稿对齐，第三条使用 `DefaultDraft` 变体显示草稿红点。
- 试套截图与源稿视觉回归通过；正式业务页面未修改。后续正式替换应按页面逐个进行，每页替换后重新检查原型触控区与连线。

## 2026-08-02 首次正式组件替换

- 正式证件文档列表 `130:2` 的三条文档已替换为 `List/DocumentRow` 组件实例，旧标题、日期、分隔线、草稿点和旧触控层已移除。
- 第一条文档原有的点击连线已迁移到组件实例，仍导航至文档详情 `133:2`；另外两条原稿本来没有连线，未虚构新的原型行为。
- 第三条文档使用深色 `DefaultDraft` 变体，草稿红点由组件内部管理。
- 校验结果：3 个实例、0 个旧散图层、0 个缺失字体，视觉回归通过。正式替换目前只覆盖这一张页面。

## 2026-08-02 正式组件采用完成

- 已将 4 个核心组件集正式应用到 14 张业务与主题画面，共 29 个组件实例；原有视觉结构保持不变。
- 底部导航已应用到首页、完整首页、日历、分类、我的、首页生活入口稿和浅色主题研究，共 7 个实例；每个变体新增四个 44×44 pt 标签触控区。
- 文档行已应用到普通文档列表、提醒池编辑、候选池 3 篇、更多菜单和删除确认背景，共 15 个实例；勾选与更多按钮均有独立 44×44 pt 触控区。
- 首页冰箱与物品位置入口已应用到完整首页和独立生活入口稿，共 4 个实例。
- 文档、食材和物品删除确认已应用统一紧凑弹窗，共 3 个实例；各自取消与删除按钮仍指向原有正确画面。
- 最终自动校验通过：14 张画面旧散图层为 0、缺失字体为 0、小于 11 pt 的可见文字为 0、触控尺寸问题为 0；文档打开、提醒池增减、更多菜单和三类删除流程的原型去向均正确。

## 下一步

1. 在可交互的 Mac、云 Mac 或真实设备上验证手势、键盘、通知权限与系统文件面板；云端截图已覆盖编译、安装、启动和首页静态视觉。
2. 在签名应用或真实设备上验证通知授权、送达与取消；备份导出系统面板和其余核心 UI 冒烟已经通过。
3. 导航动效继续后置；正式引入前必须在可交互设备上确认，不阻塞第一版核心功能。

## 2026-08-02 GitHub macOS 自动编译准备

- 已新增共享 `MemoryHub` Scheme，确保命令行和 GitHub Actions 能稳定发现应用 target，不依赖某台 Mac 上的个人 Scheme。
- 已新增 GitHub Actions 工作流：固定使用 `macos-15` 与 Xcode 16.4，执行无签名的 iOS Simulator Debug clean build。
- 工作流同时支持推送到 `master`/`main`、Pull Request 和手动触发；并限制只读仓库权限、20 分钟超时与同分支旧任务自动取消。
- 已创建私有仓库 <https://github.com/Glandy-T/memory-hub-ios>，本地 `master` 已跟踪 `origin/master`。
- 第一次 Xcode 16.4 编译实际暴露两类问题：带 footer 的 `Section` 初始化方式不兼容，以及数据管理页单个 SwiftUI 表达式过大导致类型检查超时；同时发现一处未使用返回值警告。
- 提交 `9ee5f37` 已修复上述问题，并将 `actions/checkout` 升级到 v5；第二次 GitHub Actions 使用 iOS 18.5 Simulator SDK 完成 Debug clean build，结果通过。
- 真实 Xcode 编译现已验证；仍未验证的是应用启动后的交互、系统权限、文件面板、通知行为和与 Figma 的视觉一致性。

## 2026-08-03 模拟器启动与首屏视觉冒烟

- GitHub Actions 已能自动选择 runner 中最新可用的 iPhone 16、启动模拟器、安装并运行 `com.glandy.memoryhub`，分别抓取浅色和深色首页截图。
- 运行 `30781448900` 使用 Xcode 16.4 完成 clean build、模拟器冷启动、安装、双主题截图和产物上传，所有步骤通过。
- 首轮真实截图发现中文界面的日期仍受模拟器英文地区影响；已改为显式中文 `DateFormatter`，最终截图稳定显示“8月2日 周日”。
- 浅色颗粒背景、深色语义色、首页空状态、文档提醒空池和冰箱/物品双入口均能实际渲染；双入口之后页面直接收口，没有设计阶段反复讨论的大段空白。
- 生产代码保留系统 `TabView` 的页面选择语义，但隐藏系统 Tab Bar，并用统一的无文字悬浮材质胶囊承载四个一级入口；二级页面仍不显示该导航。
- 为控制 macOS runner 用量，普通 push/PR 只执行编译；只有手动触发 `iOS Build` 工作流时才启动模拟器并生成截图。截图 artifact 保留 7 天，本地下载副本位于被 Git 忽略的 `.artifacts/`。

## 2026-08-03 首页材质验收

- 首页今日事项、文档提醒、冰箱和物品位置均改为统一的系统超薄材质卡片，并叠加克制的 Light/Dark 主题色与细描边；浅色颗粒会经过模糊后透入卡片，不直接干扰文字。
- 原生 `TabView` 保留，用于维持系统交互、辅助功能和二级页面隐藏逻辑；底栏改为无文字四图标，并显式使用超薄系统材质背景。
- 提交 `1ba8897` 已通过 Xcode 16.4 编译。手动工作流 `30782363966` 完成 iPhone 16 安装、启动、浅色/深色截图和 artifact 上传。
- 实际截图确认：浅色卡片呈现淡蓝、淡黄与淡粉的透色层次，颗粒仍可辨认但不会落在文字同一视觉平面；深色卡片保持低对比层级；两个生活入口后页面直接收口。
- 本地验收图位于 `.artifacts/simulator-run-30782363966/memory-hub-simulator-screenshots/`，该目录不进入 Git。

## 2026-08-03 满屏事项轮播与玻璃材质修正

- 明确区分两种状态：无事项时显示紧凑提示条；存在事项时，日期、区块标题与事项大卡共同占据首页首屏，文档提醒和生活入口位于向下滚动后的内容区。
- 多条事项使用内嵌横向 `ScrollView`、`LazyHStack`、`scrollTargetLayout` 与 `viewAligned` 吸附切换。几何以 Figma 首页节点 `7:1236` 为准：430pt 画板上主卡 310×548、侧卡 170×438、卡间距 12pt；实现按设备宽度等比缩放。有三条以上事项时初始聚焦中间卡，使左右真实相邻卡同时露出。
- 主卡标题与时间恢复到设计稿顶部区域；日期下方分隔线、“今日事项 / 待处理数”居中层级以及侧卡彩色提示块均按 Figma 恢复。后续确认的浅色颗粒和苹果玻璃材质优先于旧深色稿的纯蓝卡片填充。
- 首页不常驻显示“无视 / 完成”按钮：当前卡片上滑完成、下滑无视，超过 88pt 阈值才执行；拖动中显示方向反馈，Reduce Motion 下不播放飞出动画，并提供同名无障碍动作。
- 材质从厚磨砂调整为更接近苹果系统玻璃的 `.ultraThinMaterial`，移除明显乳白覆盖和主题染色，只保留低强度方向性高光边缘；项目继续兼容 iOS 17，不使用仅限 iOS 26 的 Liquid Glass API。
- 深色首页保留低饱和青、紫环境光，使透明材质在深色背景上仍有轻微透射层次。
- GitHub Actions 截图启动参数 `--memory-hub-screenshot-sample` 仅在模拟器视觉验收时注入 3 条示例事项，不进入普通启动路径。
- 提交 `abfd542` 已通过 Xcode 16.4 编译、iPhone 16 安装与双主题截图；最终工作流为 `30789632778`。静态截图确认主卡居中、侧卡缩小、左右露出、顶部信息层级与单层悬浮导航均正常；本地截图位于 `.artifacts/simulator-run-30789632778/`。真实纵向手势仍需在可交互设备或 Mac 模拟器上做触控验收。

## 2026-08-03 浅色事项卡内容构图提案

- Figma 新增独立提案节点 `330:55`（`首页 · 浅色事项卡构图提案 A`），未覆盖首页正式稿；确认后的构图已由提交 `0c53eae` 同步到 SwiftUI。
- 保留主卡 310×548、侧卡 170×438 和 12pt 间距；最终删去侧卡彩色色块、独立时间胶囊和卡底常驻手势说明。主卡上半部使用 48pt Regular 浅色时间，未设置时间时原位显示“全天”；中部为事项标题，标题下方仅在有内容时显示可选备注。侧卡仅露出真实玻璃卡轮廓，手势方向只在实际拖动时反馈。
- 标题使用蓝灰而非纯黑；浅色颗粒背景透过玻璃形成色彩层次，文字组保持透明，不另加白色承载条。
- 首页首屏内容高度按设备视口约束，首屏不显示文档提醒；用户需主动向下滚动后才进入提醒区。
- 本地提案截图位于 `.artifacts/figma-proposal-330-55/home-light-card-proposal.png`，该目录不进入 Git。
- 提交 `10f59e7` 为 `CalendarItem` 增加可选备注并把数据库 schema 升至 v7；新增/编辑表单支持 2–4 行备注，纯空白保存为 `nil`。提交 `eef44ed` 将最终时间层级收口为 48pt Regular，并保留动态字体缩放。
- GitHub Actions 手动工作流 `30795764710` 已使用 Xcode 16.4 完成编译、iPhone 16 安装启动和浅色/深色截图。两套截图确认大时间、标题、备注、无色块侧卡、无常驻教学文案及首屏隐藏提醒区均已实际渲染；本地截图位于 `.artifacts/simulator-run-30795764710/`。

## 2026-08-03 分类与文档流程实际收口

- 分类普通态按最终稿收口为搜索、分组标题和整行按钮式分类卡；单击进入文档列表，长按进入管理。管理态保留拖动排序、更多菜单、新增分类、改名、改色与删除。
- 文档列表普通态与编辑态均已实现：编辑态可批量勾选加入首页提醒候选池，并保留每篇文档的更多菜单和删除确认；文档编辑日期统一显示中文并带“编辑”。
- 文档详情改为扁平时间线记录，不再使用多层卡片；支持新增记录、自动保存草稿、完成发布、放弃草稿、删除记录和空草稿清理。
- 分类导航路径提升到根视图统一管理：分类根页显示悬浮底栏，进入文档列表或详情后隐藏，返回时恢复；二级导航使用简洁返回箭头，不重复显示上级标题。
- 提交 `2ae1fdf`、`bfae904`、`4cc4897` 分别完成分类、文档列表和记录编辑；`420c64b` 增加分类/文档模拟器视觉验收路线；`8ed649d` 与 `32713e9` 修复二级导航编译和底栏生命周期。
- 最终普通编译运行 `30802119123` 通过。手动模拟器运行 `30802240069` 使用 Xcode 16.4 完成 clean build、iPhone 16 启动及七张截图；最终复核确认分类根页底栏、文档二级页无底栏、中文日期和记录布局均正确。
- 最终截图位于 `.artifacts/simulator-run-30802240069/`，该目录不进入 Git。拖动排序、长按进入管理和草稿键盘交互仍需在可交互 iPhone 或 Mac 模拟器上补充触控验收。

## 2026-08-03 全局主题背景与玻璃规则修正

- 浅色彩虹颗粒从首页私有背景提升为 `memoryHubPage()` 的全局主题背景，分类、日历、我的及使用该页面修饰器的二级页面共享同一视觉语言；首页不再重复叠加素材。
- 深色主题以 Figma 分类最终稿 `7:2720` 为准：底色 `#111925`，上方为克制紫色空间光，中下部为低强度蓝青层次。环境色只服务于玻璃透射，不进入内容卡内部。
- 分类搜索框、普通分类条和管理分类条统一改用 `memoryHubGlassCard`；深色材质增加低透明度蓝灰染色，避免在纯色底上退化为不透明中性灰卡。
- 删除首页事项卡内部额外绘制的蓝—黄—红—紫渐变光晕。浅色卡片的颜色应来自全局颗粒背景自然透入，深色卡片不携带彩虹装饰。
- 私有仓库的 macOS Actions 曾被账单/支出上限拦截；公开前已检查已跟踪文件，不含密钥、个人路径、真实备份或账号信息，随后将仓库改为公开以使用标准公开仓库 runner。
- 公开后运行 `30804665759` 已通过 Xcode 16.4 clean build；手动模拟器运行 `30805448262` 完成 iPhone 16 启动与七张截图。实际截图确认浅色颗粒已进入分类页并透过玻璃卡，深色分类保留紫上、蓝青下的空间层，深色首页事项卡不再携带内部彩虹光晕。截图位于 `.artifacts/simulator-run-30805448262/`。

## 2026-08-02 第一版源码收口

- 第一版产品说明中定义的核心本地数据模块均已有 SwiftUI 页面和持久化实现：首页、日历、分类/文档/记录、提醒池、冰箱/采购、物品、归档、回收站、搜索、通知和备份。
- 数据库当前 schema 为 v7，旧版本缺失的新集合使用空数组迁移，事项备注等新增可选字段按空值迁移；导入更高 schema 的备份会明确拒绝。
- 工程包含直接可打开的 `MemoryHub.xcodeproj` 与可再生的 `project.yml`。所有 Swift 源文件和 Asset Catalog 均已加入 target。
- 已建立本地 Git 阶段存档：iOS 基线、提醒池、周期事项、冰箱与物品，以及最终第一版收口。每次存档前均执行工程引用、括号结构、JSON 和 `git diff --check` 检查。
- 当前限制：Windows 环境无法执行 `xcodebuild`、iOS 模拟器、通知权限和系统文件面板验证，因此不能把“静态检查通过”描述成“已通过 Xcode 编译”。

## 2026-08-02 原生 iOS 开发启动

- 新增 `project.yml`，以 XcodeGen 生成原生 SwiftUI iOS 17 工程；应用代码位于 `MemoryHub/`。
- 建立版本化 `AppDatabase` 与本地 JSON 持久化。写入采用原子替换；读取失败时以空白数据库启动并在界面显示中文提示。
- 默认分类使用稳定键 `system.default`，启动时会自动修复缺失的默认分类。
- 已完成首个数据闭环：新增分类 → 进入文档列表 → 输入标题创建文档 → 新增记录草稿 → 自动保存 → 完成发布或放弃。
- 正式记录的编辑草稿独立于已发布正文；完成时才覆盖展示内容，并把旧正文写入版本历史。
- 文档和正式记录删除均写入 `deletedAt`，文档删除同时移出提醒池并软删除所属记录，为统一回收站保留恢复基础。
- 文档列表编辑态可立即加入/移出提醒池；首页打开或手动刷新时从有效池子随机抽取最多三篇。
- 已建立浅色/深色语义主题入口和四栏导航。首页、日历、分类、我的均已实现；冰箱与物品由首页入口进入各自独立模块，相关新增、编辑、删除与恢复流程已经接通。
- 分类长按管理已接通：支持拖动排序、重命名、预设色修改、新增与删除；含文档分类可选择迁移到默认分类或连同文档一起进入回收站。
- 日历已支持按日期新建和编辑普通事项，标题必填、时间可选；事项可完成、无视或软删除。首页读取同一事项，并以凌晨 4 点为今日刷新边界，完成或无视后提供 4 秒撤销。
- “我的 → 回收站”已接通分类、文档、正式记录与日历事项的恢复和永久删除；永久删除必须二次确认。级联删除使用来源标记，恢复文档或分类时不会误恢复此前已经单独删除的记录。
- 文档提醒已补齐首页操作：今天隐藏、通过时间滚轮延后最多 24 小时、永不提醒（移出池）；“我的 → 文档提醒池”可查看并移除当前候选文档。首页点击提醒可直接进入原文档。
- 周期事项规则已实现：名称与开始日期必填，结束日期可选，默认每天或指定星期；日历只在用户查看具体日期时生成当天实例，不预创建大量未来记录。停止或删除规则不会抹掉历史实例，规则删除进入统一回收站。
- 首页冰箱与物品卡片已连接独立模块。冰箱支持冷藏/冷冻、数量、可选到期日、开封与备注，移除原因、用完加入采购、购买后可直接补入冰箱，以及 15 天专属历史。物品支持自由文本位置、数量、库存状态、配件、备注与位置变化历史，删除进入统一回收站。
- 分类顶部搜索已扩展为全局搜索，覆盖分类、活跃与归档文档、正式记录、冰箱、采购和物品；排除回收站，草稿不作为独立结果。
- 文档已支持归档与恢复；记录正文支持 Markdown 小标题插入和长内容约 9 行折叠。
- 数据管理已支持 JSON 本地备份导出、导入前明确选择合并/替换和存储数量概览；合并时会归一系统默认分类，避免产生两个“默认分类”。
- 通知设置已实现每日 8:00/9:00 检查提醒，以及普通/强提醒；强提醒间隔可选，事项完成、无视或删除时取消后续通知。系统能力按 iOS 通知权限执行，不承诺闹钟级行为。
- 浅色主题颗粒素材已加入 Asset Catalog，并由全局页面主题作为低透明度背景层；文字、控件和状态仍使用 Light/Dark 语义色。

## 工作约定

- 修改设计前先阅读本文件和 `PRODUCT_SPEC.md`。
- 已确认页面优先做局部修正，不因临时参考整体重做。
- 新的实验方向先放独立研究画面，不覆盖主页面。
- 每次完成一段设计或规则调整，都同步更新本文件，避免依赖单个 Codex 任务的上下文。

## 2026-08-10 Android 日历与文档分类闭环

- 日历把“当天截止”和“当日安排”整理为同一层级的光学玻璃条目；当日事项不再使用容易误读为失败的叉号，时间或“全天”固定显示在左侧，状态与编辑入口位于右侧。
- 待收录文档编辑器新增“保存到分类”，候选卡会预先显示目标分类；外部任务传入 `categoryId` 或 `categoryName` 时优先匹配有效分类，否则安全回落到“未分类”。
- 已保存文档可从分类列表编辑菜单或文档详情右上角移动分类。移动会立即持久化，已删除分类不能成为目标。
- Flutter 静态分析无问题；53 条非视觉测试通过，覆盖指定分类收录和保存后移动；430×932 首页、分类、日历三张视觉基线重新生成并通过。

## 2026-08-10 Android 全面使用审查与硬化

- 修正已配对设备启动时同步请求可能阻塞首屏约 15 秒的问题：连接信息仍立即从安全存储恢复，远端刷新改为首帧后的后台任务，保留启动、回前台和手动刷新能力。
- 补齐可逆操作：已完成/已无视事项可恢复待处理，已完成截止日可恢复进行中，采购勾选后提供即时撤销；这些状态不再只能通过回收站或重新创建绕行。
- 补齐可清空字段：待收录日程可从具体时间改回全天，冰箱条目可清除到期日；旧到期日也能重新打开日期选择器。
- 物品位置的长历史改为可拖动并独立滚动的底部面板，避免条目较多时超出屏幕；输入标题、位置、数量、容器和备注增加合理长度边界。
- 首页事项卡移除与全局主题重复的内部彩色渐变，让颜色只由共享颗粒背景透过玻璃产生；修正轮播数据减少时页码越界、窄屏底栏宽度，以及 320×700 / 1.8 倍字体下日期、日历入口和月份标题溢出。
- 分类条、文档行与首页生活入口从固定高度调整为最小高度，允许系统大字体自然扩展；长标题和备注使用明确行数与省略策略。
- 回收站计数纳入已删除截止日，避免“我的”页数量与实际列表不一致。
- 新增待收录编辑、无阻塞连接恢复、状态恢复、采购撤销、窄屏大字体回归测试。Flutter 静态分析无问题，59 条非视觉测试和 3 张 430×932 视觉基线全部通过。

## 2026-08-08 Android 分类与日历设计回归修正

- 重新读取旧任务 `阅读并分析 PRODUCT_SPEC.md` 并对照 Figma 分类节点 `7:2720`、日历节点 `7:2421`，恢复此前已确认而 Flutter 临时实现遗漏的结构与交互。
- Android 分类页恢复为：标题、玻璃搜索框、分隔线、`全部分类` 分组标题、逐条独立玻璃分类按钮。普通态不常驻“管理”入口和箭头；单击进入文档列表，长按并提供触觉反馈后进入管理态。
- Android 日历顶部恢复独立的“今天”和“周期事项”入口；月份标题位于月历玻璃板内并可打开远日期跳转，玻璃板支持左右滑动切换相邻月份。
- 当天事项列表恢复状态图标、可选时间右对齐和列表末尾居中的 `＋ 新增事项`；新增入口不再使用标题右侧的小按钮。
- 点击日历事项可编辑标题、日期、可选时间和备注；删除必须二次确认，随后将状态写为 `deleted` 并进入软删除流程，界面提供撤销。首页仍只保留完成与无视，不提供删除。
- 控制器新增事项编辑持久化、分类排序/改色/安全删除方法；快速连续创建分类的回归测试还发现并修复了时间戳 ID 在 Windows 上可能碰撞的问题。
- 逻辑和组件测试扩充到 13 条，并新增 3 条 430×932 浅色视觉基线（首页、分类、日历）。静态分析、普通测试与视觉测试均通过。

## 2026-08-03 核心单元测试基线

- 新增共享 `MemoryHubTests` 单元测试 target，并同步维护 `project.yml`、Xcode 工程与共享 Scheme；后续在 Xcode 中可直接运行，工程重新生成时也不会丢失。
- `AppStore` 支持注入数据库文件路径与独立 `UserDefaults`，正式应用仍使用原有 Application Support 与标准偏好设置；测试使用一次性临时目录，不接触用户真实数据。
- 首批 6 条测试覆盖：旧 schema 迁移和默认分类修复、文档删除/恢复时提醒池与记录级联、编辑记录版本历史、无时间事项强制禁用通知、周期事项单日去重，以及备份合并时默认分类映射和 ID 去重。
- 修正旧 Debug 配置缺少 `ENABLE_TESTABILITY` 且沿用 `-O` 优化的问题；Debug 现在使用标准 `-Onone`，Release 配置未改变。
- GitHub Actions 的 push/PR 流程已从纯编译升级为编译后在 iPhone 16 Simulator 运行单元测试；手动截图流程继续保持原有行为，避免重复启动模拟器。
- 最终运行 `30811918510` 使用 Xcode 16.4 完成编译与测试：6 tests、0 failures、`TEST SUCCEEDED`。关键交互 UI 自动化仍是下一阶段工作。

## 2026-08-03 全局玻璃层级与深色主题收口

- 解决分类条与首页大事项卡虽然共用修饰器、实际却像两套材质的问题：新增 `compact`、`standard`、`hero` 三档玻璃参数。搜索框、分类条、管理分类条和底栏使用紧凑档；首页大事项卡使用更通透的主视觉档；生活入口继续使用标准档。
- 材质差异只服务于表面积和信息密度，三档继续共享系统超薄材质、连续圆角、方向性高光边缘与克制阴影，不再用每个页面独立的纯色卡片模拟玻璃。
- 深色卡片降低了系统材质层本身的强度，而不只是削弱蓝灰蒙层，因此背景环境明暗可以真实透入；首页主卡不携带独立彩虹光晕。
- 深色背景改为连续的海军蓝—青黑基底，配合局部紫、青、蓝环境光和低饱和细颗粒。顶部不再是一整块紫色色雾，画面中下部也不再用大块蓝色填满。
- 底部导航纳入同一紧凑玻璃修饰器，首页与分类页不再各自呈现不同的边缘和阴影。
- 三个阶段提交为 `0e96e9c`、`905633f`、`1ee6978`。最终普通编译运行 `30808307667` 通过；最终模拟器运行 `30808449404` 完成 Xcode 16.4 编译、iPhone 16 安装启动与七张浅/深色截图。
- 最终截图位于 `.artifacts/simulator-run-30808449404/memory-hub-simulator-screenshots/`。静态验收确认首页、分类、管理分类与底栏的材质语言统一，浅色主题未发生视觉回退；手势和滚动的动态观感仍留待真实设备或可交互模拟器确认。

## 2026-08-07 Web/PWA 正式闭环

- Web 数据库升级到 schema v2，并在读取、同步与 Worker 校验中兼容 v1 数据；分类、周期规则、偏好与提醒池状态均随私有 D1 数据跨设备合并。
- 补齐分类、搜索、文档/记录、冰箱与待采购、物品位置、周期事项、软删除回收站、主题设置和 JSON 备份恢复；外部 intake 仍需用户确认才进入正式内容。
- 已在 430×932 和 1200×900 浏览器实际验证新增事项、重载持久化、分类新增及完成/无视交互，控制台无应用错误。

## 2026-08-11 ADHD Android 实验版最终验收

- 实验版使用独立入口、Android 包名、应用名、数据库与备份键，可与正式版并存；新增功能未合并到正式入口。
- 完成快速收集、待收录、任务关联、3/5/8 步拆解、专注计时与暂停检查点、容量时间线和常用流程闭环，并生成独立应用图标全套资源。
- Android 实测发现并修复控制器生命周期、IME 遮挡/溢出、底栏触控阻塞、低动态动画断言、实验入口过深与 Release 前台状态误判。
- 人工截图复核又发现“明天下午”被默认存为今天的语义矛盾；现已识别相对日期和常见中文时间，可在确认页修改或恢复全天，并有确定性回归测试。
- 最终 CI 运行 `31415412940` 在 Pixel 7 / Android 15 完整实操并通过，干净 Release APK 重装、启动、前台状态和崩溃扫描均通过；最终截图无裁切、黑屏、低对比或视觉体系断裂。
- APK SHA-256：`8856116A140F25FB23BA566E8D1CC8E44B8E6EE99C3498ADD8529B8C9CEE279C`。详细存档见 `docs/ADHD_LAB_ARCHIVE.md`。
