# Memory Hub — 当前进度交接

最后更新：2026-08-02

## 项目基线

- Figma 视觉准备已进入可开发状态，原生 iOS 第一阶段工程已经开始。
- 产品功能与数据规则以 `PRODUCT_SPEC.md` 为准；本文件记录当前设计状态与已确认交互。
- Figma：<https://www.figma.com/design/K2hgHAGExCF40ZtPtMY91x/Personal-Memory-Hub-%E2%80%94-Dark-iOS>
- 视觉方向：安静、清晰、留白克制；深海军蓝背景，冷蓝选中态，低饱和青紫环境光。
- 排版参考：<https://www.behance.net/gallery/242023175/UIUX-for-Wellness-Aging-Care-Mood-Meds-Tracking-app>
- 配色参考：<https://www.behance.net/gallery/210003217/Aqara-Smart-Home-App-UI-UX-Design>

## Figma 页面与关键画面

| 页面 | 页面 ID | 关键画面 |
| --- | --- | --- |
| 首页 | `0:1` | 首页主画面 `7:1236`；完整纵向内容 `251:2` |
| 日历 | `7:1291` | 月历主页 `7:2421` |
| 分类与文档 | `7:2585` | 文档列表 `130:2`；提醒池编辑 `230:4`；文档菜单 `232:8`；删除确认 `232:110`；重命名 `236:2`；文档详情 `133:2`；新建文档 `136:110` |
| 导航动效研究 | `86:2` | 仅为研究稿，当前低优先级 |
| 首页提醒与生活入口 | `106:2` | 提醒、冰箱与物品卡片 `154:2` |
| 冰箱与物品 | `164:3` | 两个独立模块的列表、编辑、选择与删除流程 |
| 我的与提醒池 | `218:2` | 提醒池管理 `218:106`；空状态 `236:220` |
| 浅色主题研究 | `260:2` | 首页浅色颗粒主题研究（单屏）`265:2` |

## 已确认的首页结构

- 首页主任务来自日历中“今天”的事项，不是独立数据源。
- 主任务保持纵向长卡和横向切换关系；不为了同时展示提醒而压缩主卡。
- 文档提醒位于主任务之后，一次展示 3 条。
- 提醒从用户明确加入的“文档提醒池”随机抽取；每次打开首页或主动刷新时重新抽取。
- 点击提醒打开原文档；单条支持今天隐藏、稍后提醒、永不提醒。
- 文档提醒之后并排展示“冰箱”和“物品位置”两个近方形入口卡片；它们是独立模块，不属于分类。
- 首页纵向内容在两个入口卡片之后结束。实现时内容高度随内容收口，只保留安全区和底部导航所需间距，不制造大段空白。
- 不再把冰箱、物品入口描述为必须通过特殊“下拉展开”才能出现；正常纵向滚动到页面底部即可。

## 分类、文档与提醒池

### 分类

- 分类普通状态为按钮式列表项；单击进入该分类的文档列表。
- 长按分类项进入管理模式。管理模式提供拖动排序、重命名、改色、删除和新增分类。
- 普通状态不提供左滑删除，避免与单击进入、长按管理形成多套手势。

### 文档列表

- 普通状态右上角有“编辑”入口。
- 编辑状态左侧复选框用于加入或移出首页文档提醒池，勾选后立即生效，不另设保存按钮。
- 编辑状态右侧省略号打开文档操作菜单，包含修改标题和删除文档。
- 删除文档必须二次确认；删除文档时一并处理其记录，并遵循回收站规则。
- 设置页的“文档提醒池”只显示已经加入池中的文档；“移除”仅移出提醒池，不删除文档。
- 空池状态引导用户回到分类中的文档列表进行选择。

### 新建与记录

- 在当前分类点击“+”，第一步只输入文档标题并创建文档容器。
- 创建后进入空文档详情；第一条记录与后续记录使用同一套新增记录流程。
- 文档详情页保持阅读与记录管理职责，不再放“加入首页提醒”开关。

## 冰箱与物品

- 首页只提供两个并排入口卡片，进入后分别使用独立页面。
- 冰箱包含“冰箱内容”和“待采购”两个分区；待采购不是一级入口。
- 物品的位置使用自由文本，不强迫用户从固定区域层级中选择。
- 删除确认弹窗保持紧凑，尤其控制上下高度。

## 我的与导航

- “我的”使用安静的分组容器，不在每个设置项下使用突兀的整宽分割线；优先通过组内间距、弱底色和必要的短分隔表达层级。
- 四个一级页面统一使用同一底部导航尺寸、位置、图标和选中态；二级页面隐藏底部导航。
- 导航动效研究暂缓，不阻塞其他页面。实际开发时再根据设备预览确认动效。

## 2026-08-02 设计清理结果

- 已从分类与首页提醒页面移除 602 个不可见旧图层，避免继续编辑时误选和文件膨胀。
- 已为文档菜单、删除确认、冰箱/物品选择器与弹窗补齐至少 44 pt 的触控区域。
- 日历可见农历文字统一提升至 11 pt。
- 最终结构扫描结果：分类、首页提醒、日历、冰箱与物品页面均无缺失字体、无小于 11 pt 的可见文字、无小于 44 pt 的反应目标、无顶层画面内的隐藏直属旧图层。

## 2026-08-02 原型连线状态

- 分类与文档页面已补齐 37 个原型入口，覆盖分类单击进入、按压进入管理、管理菜单、新增分类、文档列表、文档详情、新增记录、新建文档、提醒池编辑、文档重命名和删除确认。
- 分类页连线复查结果：无失效目标，所有带反应的触控区域均不小于 44 pt。
- 冰箱与物品页面已有 34 个有效原型入口，内部新增、编辑、选择、搜索、空状态和删除流程完整。
- “我的”页面已有设置到提醒池、提醒池返回设置的路径。
- 当前各一级模块分布在不同 Figma 页面。Figma 原型的 `NAVIGATE` 目标必须是同一页面的另一个顶层画面，因此底部导航、首页提醒到文档、首页卡片到冰箱/物品、提醒池空状态到分类编辑不能直接跨页面连线。
- 如果需要一条可从首页完整点击到底的演示原型，应新增独立的“完整原型”页面，将各模块的演示画面复制到同一页面后再连接；现阶段不为连线而复制主设计稿。

## 2026-08-02 首页完整纵向内容

- 已新增画面 `首页 · 完整纵向内容`（`251:2`），把确认过的主任务卡、三条文档提醒、冰箱和物品位置入口整合为一条连续内容流。
- 区块节奏为：主任务卡结束后 32 pt 进入文档提醒；最后一条提醒后 24 pt 进入双入口卡片。
- 双入口卡片结束后只保留内容收口和底部导航安全间距，不再复制原独立画面中的大段空白。
- 该画面是开发用的完整内容高度示意；实际 iOS 实现应使用纵向 `ScrollView`，底部导航固定或通过安全区 inset 布置，不能靠给内容硬塞空白避让。
- 视觉与结构复查：无缺失字体、无小于 11 pt 的可见文字、无内容越界。沿用现有设计字体 `Inter Semi Bold` 与 `Noto Sans SC`。

## 2026-08-02 浅色主题方向

- 已新增独立页面 `07 · 浅色主题研究`（`260:2`）与研究画面 `首页 · 浅色颗粒主题研究 · 单屏`（`265:2`），由正式单屏首页 `7:1236` 复制，未覆盖深色母版。
- 浅色主题不是简单反相：使用冷白底、深墨蓝文字、半透明白色卡片与彩色颗粒雾。颗粒是主题识别层，不照搬参考图的彩虹构图。
- 主题研究只使用标准 430×932 首屏，不再依赖完整长页面展示色彩变化。
- 已移除容易呈现为水渍的连续模糊色块，以及后续效果像脏污的稀疏矢量噪点。当前研究稿使用重新平衡构图的高密度彩粉材质图 `assets/theme/light-pigment-background-v2.png`：蓝青回到上中部，黄绿补足左中，粉红与紫色在下方左右分担重量，视觉中心落在主卡片内部；边缘由密集微粒自然消散，顶部文字区域保持较干净。
- 该画面暂为主题研究稿。确认视觉方向后，再把浅色/深色语义颜色写入设计变量并批量应用到其他页面。

## 2026-08-02 设计系统基础

- 已建立 4 个变量集合：`Memory Hub · Primitives`、`Memory Hub · Color`、`Memory Hub · Spacing`、`Memory Hub · Radius`。
- 共建立 59 个变量，其中 16 个语义颜色支持 Light / Dark 两种模式，并全部通过基础颜色别名连接；其余为基础颜色、间距和圆角变量。
- 已建立 10 个标准文字样式，覆盖日期展示、页面标题、卡片标题、区块标题、正文、标签和最小辅助信息；暂未批量覆盖现有画面。
- 已建立卡片、悬浮控件和弹窗 3 个标准阴影效果样式。
- 第一阶段校验通过：4 个集合均无模式缺值，语义颜色无别名失效。下一阶段整理底部导航、列表项、卡片和弹窗组件。

## 2026-08-02 核心组件库

- 已新增独立页面 `08 · 设计系统`（`285:2`），不直接改动现有业务画面。
- `Navigation/Bottom`（`285:131`）：Light / Dark × Home / Calendar / Categories / Profile，共 8 个变体；选中态统一使用颜色与阴影，不再依赖重复图层。
- `List/DocumentRow`（`288:46`）：Light / Dark × Default / DefaultDraft / EditingSelected / EditingUnselected，共 8 个变体；草稿红点由组件自身提供，编辑态包含提醒池复选框和 44 pt 更多操作区域。
- `Card/ModuleEntry`（`289:36`）：Light / Dark × Fridge / Location，共 4 个变体。
- `Dialog/Confirm`（`289:55`）：Light / Dark 共 2 个变体，固定 320×164 紧凑比例，双按钮各 160×64。
- 组件阶段校验通过：4 个组件集、22 个变体，数量与预期一致。

## 2026-08-02 组件试套

- 已新增独立页面 `09 · 组件试套`（`291:2`），画面 `证件文档列表 · 组件试套`（`291:3`）由正式文档列表 `130:2` 复制。
- 三条文档已替换为 `List/DocumentRow` 实例；标题、日期、78 pt 行距和分隔线与源稿对齐，第三条使用 `DefaultDraft` 变体显示草稿红点。
- 试套截图与源稿视觉回归通过；正式业务页面未修改。后续正式替换应按页面逐个进行，每页替换后重新检查原型触控区与连线。

## 2026-08-02 首次正式组件替换

- 正式证件文档列表 `130:2` 的三条文档已替换为 `List/DocumentRow` 组件实例，旧标题、日期、分隔线、草稿点和旧触控层已移除。
- 第一条文档原有的点击连线已迁移到组件实例，仍导航至文档详情 `133:2`；另外两条原稿本来没有连线，未虚构新的原型行为。
- 第三条文档使用深色 `DefaultDraft` 变体，草稿红点由组件内部管理。
- 校验结果：3 个实例、0 个旧散图层、0 个缺失字体，视觉回归通过。正式替换目前只覆盖这一张页面。

## 2026-08-02 正式组件采用完成

- 已将 4 个核心组件集正式应用到 14 张业务与主题画面，共 29 个组件实例；原有视觉结构保持不变。
- 底部导航已应用到首页、完整首页、日历、分类、我的、首页生活入口稿和浅色主题研究，共 7 个实例；每个变体新增四个 44×44 pt 标签触控区。
- 文档行已应用到普通文档列表、提醒池编辑、候选池 3 篇、更多菜单和删除确认背景，共 15 个实例；勾选与更多按钮均有独立 44×44 pt 触控区。
- 首页冰箱与物品位置入口已应用到完整首页和独立生活入口稿，共 4 个实例。
- 文档、食材和物品删除确认已应用统一紧凑弹窗，共 3 个实例；各自取消与删除按钮仍指向原有正确画面。
- 最终自动校验通过：14 张画面旧散图层为 0、缺失字体为 0、小于 11 pt 的可见文字为 0、触控尺寸问题为 0；文档打开、提醒池增减、更多菜单和三类删除流程的原型去向均正确。

## 下一步

1. 在 Mac/Xcode 完成首次编译、模拟器运行和与 Figma 的视觉回归；当前 Windows 环境无法执行原生 iOS 编译。
2. 完成分类管理模式、日历事项与统一回收站，再实现冰箱和物品模块。
3. 浅色主题目前只完成首页研究稿；其他页面继续依赖已建立的 Light / Dark 语义色，不逐页硬编码重画。
4. 导航动效继续后置，不阻塞核心数据闭环。

## 2026-08-02 原生 iOS 开发启动

- 新增 `project.yml`，以 XcodeGen 生成原生 SwiftUI iOS 17 工程；应用代码位于 `MemoryHub/`。
- 建立版本化 `AppDatabase` 与本地 JSON 持久化。写入采用原子替换；读取失败时以空白数据库启动并在界面显示中文提示。
- 默认分类使用稳定键 `system.default`，启动时会自动修复缺失的默认分类。
- 已完成首个数据闭环：新增分类 → 进入文档列表 → 输入标题创建文档 → 新增记录草稿 → 自动保存 → 完成发布或放弃。
- 正式记录的编辑草稿独立于已发布正文；完成时才覆盖展示内容，并把旧正文写入版本历史。
- 文档和正式记录删除均写入 `deletedAt`，文档删除同时移出提醒池并软删除所属记录，为统一回收站保留恢复基础。
- 文档列表编辑态可立即加入/移出提醒池；首页打开或手动刷新时从有效池子随机抽取最多三篇。
- 已建立浅色/深色语义主题入口和四栏导航。首页、日历、分类、我的已有可运行骨架；冰箱与物品当前为首页入口卡片，页面功能尚未实现。
- 分类长按管理已接通：支持拖动排序、重命名、预设色修改、新增与删除；含文档分类可选择迁移到默认分类或连同文档一起进入回收站。
- 日历已支持按日期新建和编辑普通事项，标题必填、时间可选；事项可完成、无视或软删除。首页读取同一事项，并以凌晨 4 点为今日刷新边界，完成或无视后提供 4 秒撤销。
- “我的 → 回收站”已接通分类、文档、正式记录与日历事项的恢复和永久删除；永久删除必须二次确认。级联删除使用来源标记，恢复文档或分类时不会误恢复此前已经单独删除的记录。
- 文档提醒已补齐首页操作：今天隐藏、通过时间滚轮延后最多 24 小时、永不提醒（移出池）；“我的 → 文档提醒池”可查看并移除当前候选文档。首页点击提醒可直接进入原文档。

## 工作约定

- 修改设计前先阅读本文件和 `PRODUCT_SPEC.md`。
- 已确认页面优先做局部修正，不因临时参考整体重做。
- 新的实验方向先放独立研究画面，不覆盖主页面。
- 每次完成一段设计或规则调整，都同步更新本文件，避免依赖单个 Codex 任务的上下文。
