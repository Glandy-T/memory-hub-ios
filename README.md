# Memory Hub

Memory Hub 是一个本地优先的个人记忆与生活信息管理应用。仓库现包含原生 iOS 工程，以及可在 iPhone、Android 和桌面浏览器使用的 Web/PWA 基线。产品规则以 `PRODUCT_SPEC.md` 为准，设计与开发交接以 `PROGRESS.md` 为准。

## Web/PWA 开发基线

`web/` 是当前优先推进的跨平台版本。它沿用已确认的浅色彩虹颗粒与玻璃视觉，提供今日事项、日历事项、文档提醒、分类、冰箱、物品和“待收录”工作流，可安装到手机主屏并离线打开。日历事项可新增、编辑、完成、无视和软删除；首页对同一份数据执行上下手势，并提供短暂撤销。

Web/PWA 现已覆盖文档与记录、分类与全局搜索、冰箱/待采购、物品与位置、周期事项、可恢复删除、主题和完整 JSON 备份；正式内容先保存到离线副本，再与私有同步状态合并。

其他 Codex 任务暂不直接改写正式数据，而是按 `shared/memoryhub-intake.schema.json` 生成待收录 JSON。用户在应用中导入、检查并选择接受、编辑或忽略；相同 `item.id` 重复导入不会重复创建。Web 数据在同一 ChatGPT 登录账号下通过私有 D1 跨设备同步，同时保留浏览器离线副本。

```bash
cd web
npm install
npm run dev
```

验证命令：

```bash
npm test
npm run build
```

开发说明见 `web/README.md`，协议示例见 `shared/examples/codex-intake.example.json`。

仓库内的 `codex-skills/memory-hub-intake` 是本机 Codex 投递技能。配置个人写入令牌并安装后，其他 Codex 任务可以把预约、文档、采购、冰箱和物品信息直接送入“待收录”；该入口只有新增候选权限，不能读取整库，也不能绕过用户确认写入正式内容。

## 当前开发基线

- 原生 SwiftUI
- iOS 17+
- Swift 5.9
- 本地版本化 JSON 数据文件，原子写入
- XcodeGen 工程描述：`project.yml`
- 不依赖账号、服务器或第三方运行时

## 在 Mac 上启动

当前仓库是在 Windows 环境建立的，无法在本机运行 Xcode 编译。复制到装有 Xcode 的 Mac 后，可直接打开：

```bash
cd "/path/to/Memory Hub"
open MemoryHub.xcodeproj
```

仓库同时保留 `project.yml`。工程结构变化较大时，可安装 XcodeGen 后用 `xcodegen generate` 重新生成工程。

选择一个 iOS 17 或更高版本的 iPhone 模拟器运行。应用第一次启动时会在 Application Support 下创建 `MemoryHub/memory-hub-v1.json`。

## 自动编译

仓库包含两条独立检查：iOS 文件变化由 `.github/workflows/ios-build.yml` 使用 macOS/Xcode 验证；`web/` 或 `shared/` 变化由 `.github/workflows/web-build.yml` 在 Linux 上执行协议测试和 PWA 构建。Web 改动不会再无意义地占用 macOS runner。手动触发 iOS 工作流时仍会启动 iPhone 16 模拟器并上传截图。

Windows 本地仍不能执行 Swift/Xcode 编译。GitHub Actions 现会在 push/PR 上执行 Debug 编译与核心单元测试；交互、通知、文件面板和动态视觉仍需后续在真实 Xcode 模拟器或设备上验收。

当前状态：提交 `ca75513` 已在 GitHub Actions 运行 `31083887670` 中使用 Xcode 16.4 完成 Debug clean build，并在 iPhone 16 Simulator 执行 13 条核心单元测试和 8 条 UI 冒烟测试，合计 21 条全部为 0 failures、`TEST SUCCEEDED`。数据层进一步覆盖整库替换导入、拒绝高版本备份，以及分类级联恢复不误复活此前单独删除的内容；UI 路径覆盖第一条记录草稿发布、文档删除后的回收站恢复，以及打开并取消系统备份导出面板。

GitHub runner 于 2026-08-07 升级到 iOS 26.2 后，系统文件导出器的关闭控件会在“Cancel”和“浏览”层级之间变化。普通 CI 继续验证导出器能够打开，随后终止测试应用；实际取消、文件落盘与再次导入保留给可交互 Mac 或真机，避免把不可控的系统面板文案当作产品回归。

模拟器视觉冒烟也已通过：手动工作流 `30795764710` 完成 iPhone 16 安装、启动和双主题截图。首页按 Figma 节点 `7:1236` 的 430×932 画板比例实现：主卡 310×548，侧卡 170×438，12pt 间距并左右同时露出；Figma 提案节点 `330:55` 定义卡片上半部 48pt Regular 浅色时间、居中事项标题和可选备注。未设置时间时原位显示“全天”，未填写备注时标题下方不保留占位。侧卡不使用彩色色块占位，卡底不常驻手势教学。在其他设备上按屏幕宽度等比缩放。首页不常驻“完成 / 无视”按钮，左右滑动切换事项，上滑完成、下滑无视；方向反馈只在实际纵向拖动时出现。文档提醒位于首屏之后，需向下滚动才会出现。底栏使用与 Figma 对齐的无文字悬浮材质胶囊。浅色颗粒和苹果玻璃材质属于后续已确认的主题覆盖，不退回旧深色稿的纯蓝实色卡。截图工作流通过专用启动参数注入 3 条示例事项以验收轮播；普通启动不包含示例数据。

## 已完成的第一阶段闭环

- 四栏主导航与深浅语义主题
- 稳定的系统默认分类
- 新增分类
- 分类进入文档列表
- 文档创建后进入空文档
- 文档标题修改与软删除
- 文档列表编辑状态加入/移出首页提醒池
- 首页从提醒池随机抽取最多三篇，打开首页或主动刷新时重抽
- 新增记录草稿自动保存
- 正式记录的编辑草稿不覆盖原文，点击完成后才发布并写入历史版本
- 放弃新增草稿与放弃编辑草稿
- 正式记录软删除
- 设置页切换跟随系统/深色/浅色
- 新安装默认使用浅色主题；用户选择跟随系统或深色后会持久化该选择
- 长按分类进入管理模式，支持排序、重命名、改色、新增和两种含文档删除策略
- 日历事项新增与编辑，标题必填、备注和时间可选
- 日历事项完成、无视、软删除，以及首页同一事项的完成/无视与短暂撤销
- 统一回收站按分类、文档、记录和日历事项展示，支持恢复和二次确认永久删除
- 文档提醒支持今天隐藏、延后最多 24 小时、永不提醒，以及设置页候选池管理
- 周期事项规则支持每日或指定星期、可选结束日期、停止未来生成与回收站恢复
- 冰箱内容、待采购、15 天冰箱历史，以及物品位置和位置变化历史
- 全局搜索、文档归档、Markdown 小标题与长记录折叠
- JSON 本地备份导入/导出、存储概览和本地通知设置
- `MemoryHubTests` 单元测试 target：覆盖旧数据迁移、主题选择、提醒池候选过滤与删除恢复、记录版本、无时间事项通知规则、周期实例去重、备份合并、冰箱采购恢复和物品位置历史
- `MemoryHubUITests` UI 冒烟 target：使用隔离的临时数据库，覆盖新增文档、加入提醒池、新增日历事项、新增冰箱食材和带自由文本位置的物品
- 浅色首页颗粒主题资源已加入 Asset Catalog

## 下一阶段

1. 在可交互的 Mac、云 Mac 或真实设备上核对手势、键盘、通知权限与文件导入导出面板。
2. 在签名应用或真实设备上验证通知授权弹窗、通知送达和取消；GitHub 无签名模拟器无法稳定返回通知权限结果，不作为阻塞 CI。
3. 导航动效后置到具备可交互设备验收条件时再处理。
